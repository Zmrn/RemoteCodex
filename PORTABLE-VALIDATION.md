# 0.7.0 单 EXE 本机验证

验证日期：2026-09-08。Windows x64 本机测试，使用已运行的官方 ChatGPT 桌面端。未在第二台实体电脑或干净 Windows 虚拟机上验证。

| 检查项 | 结果 | 实际验证方式 |
|---|---|---|
| 仅复制一个 EXE | 已通过 | 全新目录只放 EXE；缓存、数据使用另一个独立目录 |
| 不依赖预装 Node/Python | 已通过 | 子进程 PATH 仅包含 Windows System32；从 EXE 解出的 Node.js 22.19.0 和 Python 3.13.2 启动 |
| 首次解压及中文、空格路径 | 已通过 | EXE 和数据目录都包含中文及空格；内置资源与文件校验通过 |
| Windows 密钥存储 | 已通过 | 内置 Python 对测试随机值执行 DPAPI 加解密，未使用或导出官方凭据 |
| 连接官方桌面 | 已通过 | Win32 管道发现、官方所有者连接、项目/任务列表及一条真实任务读取；未发送模型消息 |
| 网页 UI | 已通过 | 打包资源启动的服务由 Edge 无头浏览器读取；输入框可编辑，无 JavaScript 页面错误 |
| 仅回环监听 | 已通过 | 自动分配 `127.0.0.1` 端口；远程监听字段为空 |
| 重复启动 | 已通过 | 连续启动两次保留同一个桥接进程 |
| 移动 EXE、保存设备名称 | 已通过 | 仅修改隔离配置中的设备名称，停止后从另一目录的 EXE 启动，名称保留 |
| 停止对象校验 | 已通过 | 进程路径与 HTTP 实例 ID 一致才停止；故意错配实例 ID 时返回失败且原桥接服务仍可读取 |
| 缓存被修改 | 已通过 | 修改隔离缓存中的 HTML 后拒绝启动，恢复原字节后可继续启动 |
| 既有开发环境 | 已通过 | 原来 43127 预览仍可访问，官方进程 ID 保持一致；没有测试写入或中断开发任务 |
| 后端回归 | 已通过 | `npm test`，24 项通过 |
| 第二台电脑、远程 Tailscale 监听 | 未测试 | 本轮只做单机回环验证，没有部署或修改网络配置 |
| 无头验证之外的 Windows 窗口操作、未安装 Edge 的电脑 | 未测试 | 本轮浏览器验证采用 Edge 无头模式 |
| 官方任务写入、生图、审批 | 未测试 | 本轮只验证打包及只读连接；此前源码版本的功能证据不能代替另一台电脑的验证 |

源码复测入口：`python scripts/verify_portable.py dist/RemoteCodex-0.7.0-windows-x64.exe`。它会自动创建新的隔离目录，仅操作本程序的测试配置，默认结束时停止该测试桥接器。测试报告写入 `evidence/portable-verification.json`，原始报告包含本机路径，已从 Git 和 EXE 中排除。

若还要检查 UI，可给上面的命令加 `--keep-running`，随后执行 `node scripts/verify-portable-ui.mjs`（需要 Playwright Core），最后用 EXE 的 `--headless --home <报告中的 testHome> --stop` 停止隔离桥接器。UI 脚本阻断任务写请求和官方窗口导航，不向任何任务发送消息。

构建清单和最终 EXE 的 SHA-256 见同目录生成的 `.build.json`。运行时来源与固定摘要在 `scripts/build_portable.py`；Node 压缩包摘要另与官方 HTTPS `SHASUMS256.txt` 核对。包内没有官方应用、登录凭据、设备配置、任务记录或附件。

单文件指分发只需一个 EXE；运行时仍会解压缓存，后台运行独立的桥接 Node.js 进程，并使用系统 Edge 显示 UI。桥接进程连接现有官方桌面，不启动第二套 Codex 后端。
