# Windows 单 EXE 桌面版 0.10.0

界面统一使用深蓝灰色主题，侧栏、消息区、输入框、菜单和设备设置都保持深色。长文字发送或加入队列成功后输入框恢复默认高度；发送失败保留原文，切换会话和取回队列草稿时自动匹配文字高度。

双击 `RemoteCodex.exe` 打开应用，文件名不带版本号；当前软件版本显示在界面左下角设备名旁，切换设备不改变这个本程序版本。现在使用应用内嵌窗口：**点击关闭隐藏到 Windows 右下角托盘；右键托盘选择“退出 Remote Codex”才真正退出**。点击托盘图标或再次双击启动器会恢复原窗口。重复打开会定位现有窗口，不产生第二套服务。能确认身份的旧版残留桥接实例会在打开新版时自动停止，无需手工执行 `--stop`。

窗口、Node 桥接进程、Python 和 WebView2 子进程受同一个 Windows 进程作业管理；主程序正常退出或崩溃时，Windows 清理它的子进程。内部仍有多个运行组件，但都由同一个桌面应用负责启动和退出。官方 ChatGPT 进程不属于这个作业，不会被停止。

0.9.5 的重复启动保护覆盖不同 EXE 副本、版本和 `--home` 数据目录：同一 Windows 用户只允许一个桌面实例，后续启动聚焦已有窗口。诊断和更新预检是短暂进程，不创建另一套桌面服务。

问答现在显示选项、输入框和回答结果；运行中的异步回答直接送入同一轮。图片消息显示正文、附件预览和“下载原图”，源文件已删除时显示不可用。访问权限可在新建前预选；首次使用某个权限模式会在官方桌面准备一个可复用的 `RemoteBridge-Settings-*` 配置任务，用它传递首轮权限，真实任务不复制配置任务的历史。该配置任务属于官方应用，只执行固定的无文件操作确认消息。若配置任务未加载，准备过程可能切换官方桌面页面。

推理强度菜单的闪电切换官方 Fast/Standard 档位；GPT-6 Astra 对应 2× speed，其他支持的模型显示其档位。档位可用性来自本机官方模型缓存（最多使用 24 小时），实际开关状态来自官方会话实时流。Fast 会增加用量。当前官方新建接口没有首轮速度参数，所以闪电需创建会话后使用；不会假装首轮已加速。同步阻塞式问答已接转发并通过模拟校验，本次真实问答测试覆盖异步问答。

运行中的已加载会话也可调整模型、推理强度、访问权限和 Fast 档位，直接交给官方任务所有者保存为下一轮设置，不中断或重新发送当前任务。连接中断或官方状态未知时禁用设置。新建前和未加载历史会话仍可预选；已加载会话不会只保存本地预选而误报官方已接受。

会话首屏按最多 40 项 / 约 128 KiB 消息体分段读取（单个超长消息仍保留完整内容）。向上滚动或点击“加载更早的消息”继续读取；刷新不会丢掉已加载历史。图片接近可见区域时单独下载。分页查看缓存有数量、内存和时间上限，服务重连后自动重新建立分页游标，保留已显示内容；长时间未用的旧缓存过期时可点重新连接。官方历史不会被改写。双方都更新到 0.9.8 才能获得完整减量效果；连接旧服务仍可读，但旧端继续发送原来的较大响应。

隐藏到托盘或最小化时，其他设备仍可连接；右键托盘退出后，其他设备无法再通过本桥接器连接此电脑。官方正在执行的任务继续运行。

0.9.9 会在断线后自动重试，最长重试间隔 30 秒，事件通道无数据 45 秒后重新连接。首次连接失败也继续尝试；恢复时重新订阅并读取同一任务，保留草稿、图片和历史。仅重试查看连接，不重发消息、队列、审批或设置。官方应用需要保持运行；从托盘退出 Remote Codex 后停止重试。

已加载会话的输入框支持 Ctrl+V 粘贴 PNG/JPEG/WebP 图片（单张 5 MB 以内），保留输入框里的文字。新建首条和未加载会话仍需先发送文字；剪贴板普通文本粘贴保持原有行为。

会话里的文件附件和助手给出的本地文件链接可下载原文件（单文件最多 256 MB），消息旁和“结果文件”都提供入口。仅提供已读取消息中的准确文件引用；不会开放任意路径下载。更早的附件需先向上翻阅；源文件已删除或移动时会明确失败，不生成替代文件。Windows 下载时弹出保存位置选择框。

点击会话、待发送或队列中的图片可打开全窗口预览，切换“原始尺寸”查看细节并滚动，切回“适应窗口”查看全图；点击图片外的背景、右上角关闭按钮或按 Esc 返回，预览内可下载原图。

0.9.12 修复“状态变化但正文停在旧消息”：官方历史接口返回的轮次可能落后于同一会话所有者的实时状态，现在先合并实时新增轮次，再按原有 40 项上限分页。此修复需要被控制的电脑也更新到 0.9.12。

## 运行条件与数据

需要 64 位 Windows 10/11、.NET Framework 4.7.2+ 和已安装的 Microsoft Edge WebView2 Runtime。本机已验证 Runtime 152.0.4191.66。若另一台电脑缺少 Runtime，会报告界面启动失败并清理本程序的运行组件，不自动安装系统软件。

EXE 内置 Node.js 22.19.0、Python 3.13.2、WebView2 SDK Loader 和托管组件。分发只需一个 EXE；运行后会解压到 `%LOCALAPPDATA%\RemoteCodex\versions\`。设备配置、密钥和 UI 数据保存在 `%LOCALAPPDATA%\RemoteCodex\data\`。操作本机任务仍需要已运行并登录的官方 ChatGPT；当前写入适配版本 `26.901.6511.0`。不启动独立 Codex 后端。

设备名称、端口和 DPAPI 密钥沿用旧版配置。0.9.0 内嵌窗口的草稿会在关闭、更新后恢复。0.8.x 的独立 Edge 窗口使用不同的浏览器存储，未发送草稿不保证迁入新窗口；旧浏览器资料保留，已发送消息仍在官方任务中。

## 本机接入与更新

左下角设备菜单 → 编辑设备，可查看/复制已分配的 Tailscale IP，修改访问端口，生成或自定义访问密钥。勾选允许连接并保存后开放 Tailscale 入口；默认仅回环访问。不会改变 Tailscale、代理、DNS 或防火墙。

远程接入状态页始终显示配置的访问端口，并明确标注是否正在监听；点击“设置本机接入”可直接进入本机设备编辑。仅复制密钥不会开启接入，需要在被控制电脑上勾选允许连接并保存。连接失败会区分端口拒绝、超时与访问密钥验证失败；连接失败不再被误写为旧版本不支持更新。

左下角问号面板顶部直接提供检查更新、安装更新和自动更新开关；有新版时问号变为下载提示，下载时显示实际百分比及小进度条。设备编辑中仍保留该设备的更新管理。应用运行时启动后检查一次，此后每小时检查；有未发送草稿或正在编辑设置时自动安装等待。隐藏窗口时继续检查；从托盘退出后停止检查。

更新先校验发布签名、平台、版本、长度和 SHA-256，再保存草稿、退出旧窗口、替换 EXE、打开新版窗口。更新助手只在安装期间短暂运行，不作为常驻服务。安装失败会尝试回退本机备份。更新源只分发软件，不接收任务、图片或账号数据。

资源服务地址和目标目录由被忽略的 `release.local.json` 配置，要求能连接该地址的 Tailscale 网络。远端只保留一个 `RemoteCodex.exe`、一个 `RemoteCodex.apk`，以及各自的签名清单。每次更新提高版本号，同时构建并覆盖发布双端；详细约定见 `AGENTS.md`。

## 开发与诊断

```powershell
# 先从 release.example.json 创建并填写 release.local.json
python scripts/build-release.py
# 发布到本机配置指定的资源目录，必须同时具备 APK 和 EXE
python scripts/publish-update.py
# 只读诊断
.\dist\RemoteCodex.exe --self-test
# 可选：停止本桌面实例（平时使用托盘右键退出）
.\dist\RemoteCodex.exe --stop
```

`--home <目录>` 隔离缓存和设备数据；`--port <端口>` 固定本地 UI 回环端口。`--headless` 仅为兼容 0.8.x 更新助手保留，普通启动调用会转到可见桌面，不再留下无窗口后台。`--prepare-only` 只校验解压，用于更新预检。

发布私钥仍为开发电脑 `data/release-signing-key.json`，以当前 Windows 用户 DPAPI 加密。不要提交、上传、打包或重新生成；公钥随客户端分发。EXE 未做 Windows Authenticode 签名，更新清单使用独立 RSA-SHA256 签名。包内第三方许可证见 `THIRD_PARTY_NOTICES.md`。
