# 更新版本展示

2026-09-10 源码修复，尚未构建发布。原更新页把当前版本与阶段提示拼在一句话里，Android 下载结束只显示“下载完成”，无法判断安装包版本。

帮助页显示当前控制端，设备设置显示指定目标端；两者通过 `public/update-view.mjs` 共享版本行、阶段、检查时间、安装按钮和旧包提示。

| 显示 | 来源 |
| --- | --- |
| 当前安装 | 正在运行的程序版本 `currentVersion` |
| 远端最新 | 最近成功验签的 GitHub 更新清单 `latestVersion` |
| 正在下载 | 本次下载固定清单的 `downloadVersion`，不随后续远端检查改变 |
| 已下载 | 完整安装包通过校验后得到的 `downloadedVersion` |
| 上次确认 | 清单成功获取并验签的 `checkedAt`，失败不更新时间 |

例如当前安装 0.10.29、包为 0.10.30、远端为 0.10.31 时，三者分别展示，并提示已下载包较旧，按钮为“下载并安装 0.10.31”。同版时按钮为“安装 0.10.30”。“与最新版一致”只指与上次确认的清单一致；用户可重新检查，不能承诺检查之后没有新发布。

`packageState` 为 `none`、`unverified` 或 `verified`。缺失包显示暂无；未完成下载、缺失或损坏清单、错误签名、包校验失败都不能宣称已校验。旧端未返回独立包版本时显示“版本未确认”，不能从 `waiting` 或 `latestVersion` 猜测。

Windows 完整候选包与自己的签名清单保存在更新缓存 `ready.exe` / `ready-manifest.json`。重启或文件属性变化后重新验签及校验大小/SHA-256/MZ；文件标识和时间使用完整整数精度，未变化时最多复用当前进程校验观察 60 秒，避免每次页面刷新重复读取整个 EXE。失败结果不缓存，避免粗粒度文件时钟下修复后的包仍显示未知。安装前既有独立验签与校验继续执行。Windows 的远端检查记录仍是本进程观察，重启后显示尚未确认，直到再次成功检查。

Android 在现有 updates 偏好中保留 `readyEnvelope`，不再把未验签的旧 `latest` 偏好或 APK 文件存在视为版本证明。启动时验签清单并核对完整 APK 哈希、包名、版本和原安装证书；旧缓存只有最新清单时也必须实际核对。检查到新版不会重命名旧包的版本。进程中候选文件大小/修改时间变化使旧校验观察失效；安装仍重新校验并由 Android 系统确认。新字段仅关联更新缓存，不改设备、密钥、任务、草稿或官方数据。

正在检查时禁用安装，迟到的检查结果不能覆盖用户操作返回的新状态。设备切换立即清空旧版本显示，旧请求/保存草稿期间的切换不能把安装请求发到另一设备。

## 验证

- `npm test`：217 项通过，新增 5 项覆盖生产下载、真实隔离 RSA 签名、旧包/新清单、重启重新校验、损坏/错配/缺文件/缺元数据、失败保留旧包及检查时间、旧协议和各阶段展示。
- `npm run compatibility`：通过，官方兼容清单未改。
- `scripts/verify-update-status-ui.mjs`：通过生产资源服务与实际共享 DOM/控制器执行 8 组隔离 API 检查；390/1300 像素无横向溢出、Android/Windows 范围、按钮版本/草稿保存、迟到请求与设备切换通过。已接入 GitHub Checks，不触发 APK/EXE 构建。
- `scripts/verify-desktop-ux-ui.mjs`：10 组原共享界面回归通过，包括帮助页入口、更新进度、草稿、图片和附件。
- `scripts/verify-update-network.py`：22 项生产网络辅助类主机 JVM 检查通过。
- 生产 `Updates.java` / `UpdateNetwork.java` 对 Android API 35 编译通过，外围 App/Activity 使用仅用于编译的替身；这不是 APK 或设备行为验证。

本次没有构建 APK/EXE、发布、升级现有客户端或执行 APK；用户需要下一次明确要求打包发布，才能安装包含此修复的版本。
