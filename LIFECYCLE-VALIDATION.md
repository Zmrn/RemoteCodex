# 0.9.0 窗口与进程生命周期修复

2026-09-08，Windows 本机，使用官方 ChatGPT 已运行进程，只读连接，不发送测试任务、不操作开发任务队列。

旧版打开 Edge 窗口后启动器立即退出，Node 服务继续驻留；换 EXE 时因版本不同拒绝复用，所以出现要求手动 `--stop` 的错误。新版由 EXE 内嵌 WinForms/WebView2 窗口，运行组件和主进程绑定 Windows Job Object，关闭或崩溃后由系统清理子进程。旧实例必须通过缓存路径、PID 与实时 API 实例 ID 验证后才会自动停止；不会按程序名字杀进程，不涉及官方 ChatGPT。

| 验证项 | 结果 | 证据 |
|---|---|---|
| 真实窗口读取官方任务 | 已通过 | 本机 WebView2 Runtime 152.0.4191.66；实际窗口显示官方任务，官方进程保持不变 |
| 点击右上角关闭 | 已通过 | computer-use 操作真实关闭按钮；9 个测试进程退出，回环端口不可达 |
| 主进程异常退出 | 已通过 | 只终止隔离测试主 EXE；包括桥接器和 WebView2 在内的 9 个进程全部退出 |
| 旧版后台自动接管 | 已通过 | 先启动 0.8.1 无窗口服务，再直接打开 0.9.0；旧进程退出，新窗口成功运行，无须 `--stop` |
| 重复启动 | 已通过 | 同一个数据目录第二次启动复用现有窗口和桥接进程 |
| 0.8.1 旧更新器升级 | 已通过 | 真实签名清单及已校验缓存包；0.8.1 → 0.9.0，原端口、新窗口，结果 updated；退出后新版 9 个进程全部结束 |
| 新窗口自动升级 | 已通过 | 原生窗口测试版本 0.8.9 → 0.9.0；安装助手独立于旧进程作业完成替换，打开新版窗口；同端口，结果 updated |
| 常规关闭后的草稿恢复 | 未测试 | 已实现限时保存和复用空闲的旧回环端口；最后的界面复测因用户按 Escape 停止 computer-use 而中止 |
| 旧 Edge 浏览器草稿迁移 | 未实现 | 0.8.x Edge 资料保留，但不保证将未发送草稿迁入 WebView2；设备名称、端口、密钥仍使用原配置 |
| 其他实体电脑、缺少 WebView2 的机器 | 未测试 | 新窗口需要系统 WebView2 Runtime；不自动修改安装或系统网络配置 |

本轮更新测试复用与服务器签名一致的本地缓存 EXE，重点验证窗口退出、EXE 替换与新窗口启动；不把它记录为又一次完整的跨网络大文件下载测试。30 项原有回归测试通过。真实关闭按钮测试后，收尾补充了草稿保存最长 1.2 秒等待与重复关闭保护；用户停止界面自动化后没有再进行窗口输入。

本机原始证据位于 `evidence/lifecycle-verification.json`、`evidence/desktop-update-legacy.json` 与 `evidence/desktop-update-native.json`，不上传 Git。测试不导出官方登录数据、私人会话或附件。构建包含 Microsoft WebView2 SDK 原始许可，固定 SDK 包摘要，实际 WebView2 Runtime 使用系统安装。

复测脚本：`scripts/verify-lifecycle.py`（prepare / closed / crash / migrate）、`scripts/verify-desktop-update.py`。窗口点击阶段由用户或 computer-use 执行；脚本只读取进程信息、启动隔离实例和注入主进程退出故障。与前期脚本一致，读取 43127 的只读源码预览作为官方进程对照；预览未运行时应先建立独立对照服务。

关闭 Remote Codex 后，其他设备不能再通过它访问本机；保持打开或最小化可继续接入。关闭本程序不会中断官方正在执行的任务。更新助手只在安装期间短暂独立运行，不作为常驻后台。
