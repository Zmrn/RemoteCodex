# Windows 程序缓存恢复

2026-09-18：针对死机重启后桌面启动报“程序缓存校验失败”，加强源码中的缓存写入和自动恢复。当前版本号仍为 0.10.41；这是未打包的源码改动，已经安装的 0.10.41 不含以下自动恢复逻辑。

现场确认：桌面 EXE 与正式发布文件哈希一致，解压后的一个 Python 脚本和 Python 标准库 ZIP 文件大小正常、内容却全部为零。用户确认发生过死机重启，Windows 也有非正常关机记录；没有证据确定文件损坏的具体环节，不能据此诊断硬盘故障。

## 启动与恢复

- 每次使用缓存都校验内置清单中的全部文件，缺失、截断或哈希错误不能执行。
- 首次解压和修复都先验证 EXE 内置 ZIP 的 SHA-256，再写入独立临时目录。每个文件使用 WriteThrough 并执行 Flush(true)，请求 Windows 将内容落盘。
- 全部文件与清单一致后，同卷重命名发布完整目录，并再次验证；发布使用 MOVEFILE_WRITE_THROUGH。未完成的 `.extract-*` 目录永不作为运行目录。
- 原版本缓存损坏时，另建同版本、同资源哈希的 `.recovered-XXXXXXXX` 目录。原缓存保持原样，运行中的旧进程无需被停止或修改；之后启动复用已完整校验的恢复目录。
- 用户与缓存目录绑定的跨进程互斥锁覆盖普通启动、自检和更新预检。并发启动只生成一份恢复缓存；进程被强制结束后，下一进程接管互斥锁并重新校验。
- 临时文件占用只做短暂重试，无法读取或权限不足不当作内容损坏。内置资源损坏、目录链接、越界路径、重复/缺失清单等继续拒绝，不绕过校验。
- 恢复路径使用短后缀，发布前检查 Windows 路径长度。磁盘不足或恢复失败会保留原目录并说明错误，不在同次启动无限重试，也不覆盖文件。

缓存模块只处理 `home/versions`。不读取、清空、迁移或重写 `home/data` 中的设备、密钥、草稿、额度历史等数据；不访问或改写官方任务。损坏目录和中断的临时目录保留以便排查，不自动执行或清理它们。

这些措施降低异常断电留下不完整文件的概率，并允许从完好的 EXE 恢复程序。它们不能保证物理存储、操作系统或硬件完全遵守落盘请求；EXE 自身损坏时仍需重新下载。真实断电没有人为复现。

## 验证

`python -X utf8 scripts/verify-portable-cache.py` 编译生产缓存模块、实际 PortableLauncher 和测试代码到隔离的 C# **测试 DLL**，不会构建应用 EXE/APK，也不运行桌面或连接官方应用。已加入 GitHub Checks。

15 组检查覆盖：正常解压与复用、实际启动器调用、两个全零文件与占用中的旧缓存、重复损坏、文件缺失/截断、共享冲突、内置资源损坏、清单不符、孤立临时目录、越界/ADS/设备路径、发布前校验、解压中途强杀进程、四进程并发、目录 junction 拒绝。合成 data 哨兵保留，强杀只针对测试自身的 PowerShell 子进程。

可传入已下载的正式 EXE 路径做额外验证：

```powershell
python -X utf8 scripts/verify-portable-cache.py <已下载且已验签的RemoteCodex.exe>
```

此项只读取 EXE 的内置资源，不调用其 Main。已用 GitHub Build 35315332244 的 0.10.41 完整资源验证实际解压、两文件置零后的恢复及再次启动复用，共 16 组通过。Node 22.19.0 的 323 项回归、兼容清单、隔离真实 HTTP/UI/DPAPI 设备保存和强制重启/删除也通过。

实现：`windows/PortableCache.cs`，由 `windows/PortableLauncher.cs` 的 Extract 共用。构建脚本已加入该模块；未来用户明确要求打包后，仍只通过 GitHub 同次生成 APK/EXE。现有客户端和未完成 Chat 工作保持不变。
