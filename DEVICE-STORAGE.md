# 设备配置恢复与持久化验证

## 0.10.15：独立历史与升级保护（2026-09-09，双端已发布）

用户报告笔记本和公司电脑重启后相互添加的设备均消失。笔记本现场的主配置与 `.bak` 都有效、修订均为 8，但只含本机，运行接口也只返回本机；因此本次不是仅有界面缓存不一致。文件时间与 0.10.14 更新启动接近，不能由此认定更新是实际覆盖来源。旧版整份内存写回的缺陷已经在 0.10.7 修复，旧记录不能作为本次原因的证明。用户明确选择重新配对，不继续恢复旧备份。

本次修复可验证的保护缺口：

1. `agents.json.bak` 是最新状态的滚动副本。现在增、删、改先在 `agents.json.history/` 提交独立历史，再更新两个滚动副本。历史记录带完整性校验、修订、操作类型、时间、进程和软件版本，只保存 DPAPI 加密密钥。历史不被普通选择或升级覆盖，不自动清理。
2. 启动服务开放 API 前，在同一系统文件锁下保全已有配置；不会改写旧主文件。读取选择最新已提交状态，旧有效副本回退或双副本损坏也能恢复设备。历史恢复后的当前选择可能回到最近一次设备增删改时的选择。
3. 最新历史损坏、同修订冲突或更高修订出现没有历史记录的删除时停止写入，保留证据并报错，不能默认重置或猜测删除意图。显式删除会先提交历史，因此不会被旧备份复活。
4. 写入检查当前修订和操作范围。普通保存不能删设备，也不能修改目标 ID 以外的设备。重复选择当前设备不重写主/备文件；读取或连接失败不删除设备。
5. 从本版本开始，升级助手在旧进程退出后、EXE 替换前保存 `updates/device-backups/` 独立快照。新进程启动后核对设备列表、加密密钥和原接入/更新设置。核对失败触发更新失败处理，不能报告成功；不会为回滚程序而用旧数据整份覆盖新数据。

验证入口：

```powershell
node --test test/agent-storage.test.mjs test/device-update-snapshot.test.mjs test/agents.test.mjs
node scripts/verify-agent-storage-ui.mjs
node scripts/verify-agent-restart.mjs
```

`verify-agent-restart.mjs` 使用独立数据目录、真实 HTTP/共用 UI/DPAPI 和独立 Windows Node 进程，强制结束后重启；`REMOTE_BRIDGE_PREVIOUS_ROOT` 可指定旧的已安装运行组件，以验证旧代码保存→新代码启动，`REMOTE_BRIDGE_CURRENT_ROOT` 可指定最终包内运行组件。脚本核对正式安装的设备/接入配置字节未变，不连接官方任务。已完成的隔离验证覆盖：旧副本同时回退、双副本损坏或缺失、进程并发、写入中断、删除不复活、历史不可用时保留原文件、UI 保存/切换/删除与跨进程重启。

### 最终合并包验收

- 已同时包含设备保护和 VS Code 共存修复，Windows EXE 与 Android APK 均为 0.10.15。130 项 Node 回归、13 项窗口检查、兼容清单检查、6 项设备 UI 检查通过。
- 最终 EXE 包内 Node/Python、DPAPI 和官方只读自检通过；以 0.10.14 包内代码保存合成远程设备，再由最终 0.10.15 包内代码启动、强制结束并再次启动，设备内容与加密密钥保持一致；显式删除后重启不复活。测试过程没有改动正式安装配置。
- 双端清单签名、完整文件大小/SHA-256、兼容清单及发布说明哈希通过线上核对；两包均不含用户配置或签名私钥。最终 EXE 为 44,060,160 字节、APK 为 203,541 字节；精确哈希见 VSCODE-COEXISTENCE.md 的本次发布记录。
- 笔记本正式安装通过内置更新器从 0.10.14 升至 0.10.15，桌面 EXE 与线上包一致。升级时现存设备为 1 项本机；其内容、选择项、DPAPI 加密密钥、接入和更新设置保持一致，并生成独立历史。远程设备保留由上述隔离合成设备测试覆盖，没有把现场仅本机的结果说成已找回旧设备。
- 网络下载中断后正常退出并重开 Remote Codex，向更新缓存提供已经与线上签名/哈希核对一致的本地产物；更新器仍自行验签、替换并报告成功。官方进程未重启，连接恢复。此次从旧版升级使用的是旧版更新助手；新增的升级前快照保护由以后运行 0.10.15 助手的更新执行。
- 本机没有运行最终合并 APK 的模拟器或真机行为测试；Android 原生存储没有修改。原开发设备合并前的 Android 兼容性行为记录不能替代本机最终包验证。

范围说明：本次定位 Windows 设备列表的数据保护，Android 原生存储不更换格式。没有进行实际断电或磁盘损坏实验；同盘历史不能保证整盘或整个数据目录丢失后的恢复。本次没有证据可以断言原始丢失的具体写入者。

## 0.10.7 历史记录

## 现场结论

2026-09-09 用户报告电脑死机重启后，设备列表只剩本机。

- **已通过：确认配置仍在。** `%LOCALAPPDATA%\RemoteCodex\data\agents.json` 包含本机和原笔记本，笔记本的稳定 ID、地址、端口与 DPAPI 加密密钥仍在。调查前已将原文件保全到被忽略的 `work/device-recovery/`，没有把密钥或私人记录加入 Git。
- **已确认异常：内存与磁盘不一致。** 运行中的 0.10.6 `/api/agents` 仅返回本机；进程环境指向上述同一数据目录，安装代码与源码一致。同环境的只读子进程能够读到两台设备。未发现当前 JSON 损坏的证据。
- **已通过：恢复真实连接。** 通过经过实例核验的 `/api/stop` 退出 Remote Codex，再打开桌面原有 EXE，设备列表恢复两项；使用原笔记本 ID 和原有密钥调用其 `/bridge/status` 返回 `connected: true` 及官方 owner PID。没有重建设备，没有换密钥，没有重启官方 ChatGPT。
- **部分通过：触发原因。** 已定位旧代码只在构造时读配置、切换设备却写回整份内存快照的缺陷，并用两个独立实例复现覆盖风险。但是无法由当前证据确定这次死机前后何时、为何形成了那份仅有本机的内存快照；不能把它断言为断电损坏或实际删除。

## 修复

1. 设备列表、设备查询及每次修改都重新读取磁盘；写入不再依赖启动时的旧列表。
2. Windows 使用系统持有的文件字节锁串行化不同进程的修改。进程退出时系统释放锁，残留 `.lock` 文件不代表仍被占用，也不需要删除它。
3. 配置附带递增修订号和 SHA-256 完整性校验。临时文件先 `fsync`，再原子替换。主文件和 `.bak` 保存同一完整版本；若保存中途退出，读取修订号最大的有效副本。
4. 主文件损坏或缺失时读取备份并提示；两个副本都无效时明确报错，保留原文件，拒绝写入默认列表。权限错误不会当作“首次启动”。
5. 成功删除设备后，两份配置都包含删除结果；其他旧实例不会将其恢复。备份写入成功但主文件失败时，提示刷新确认，避免重复新增。
6. Windows/Android 共用界面打开设备菜单时刷新列表，并保留菜单状态与焦点。读取失败保留当前显示内容并给出错误。

Windows 密钥继续使用 DPAPI，所有持久化副本都只包含加密密钥；公共接口不返回密钥字段。Android 的原生 Keystore / SharedPreferences 格式不变，APK 同步更新共用界面。

## 验证与复测

| 项目 | 结果 | 证据 |
|---|---|---|
| 旧实例读刷新、切换不覆盖、删除不复活 | 已通过 | `test/agent-storage.test.mjs` |
| 主文件损坏、缺失、可解析 JSON 被改动、备份恢复 | 已通过 | 同上，包含 DPAPI 解密一致及无明文密钥检查 |
| 双副本损坏、访问拒绝 | 已通过 | 显式失败且保留原文件 |
| 保存中途主文件替换失败 | 已通过 | 注入 I/O 故障后读取最新备份，保留删除结果 |
| 四个独立进程并发保存 | 已通过 | 全部设备及稳定 ID 保留 |
| 锁持有进程被强制终止 | 已通过 | 下一次保存直接成功，无手工清锁 |
| 旧版无校验配置升级、BOM | 已通过 | 稳定 ID 不变 |
| Node 回归、Windows 尺寸回归 | 已通过 | 89 项 Node；13 项窗口检查 |
| 桌面和手机宽度的浏览器菜单 | 已通过 | `scripts/verify-agent-storage-ui.mjs`，5 项，无页面异常 |
| 原有跨设备切换与断线恢复 | 已通过 | `scripts/verify-device-switch-ui.mjs`，8 项，无任务写入 |
| 单 EXE 运行时与官方只读接口 | 已通过 | 隔离 `--self-test`，DPAPI、官方项目与任务查询通过 |
| 打包后的设备存储模块 | 已通过 | 使用包内文件锁助手和 Python，验证双实例保存、备份解密恢复、删除持久化 |
| Android 15 隔离模拟器 | 已通过 | `stage=device-storage`，菜单刷新、实例重建读取和原有设备保留；没有安装到物理手机 |
| 双端正式资源发布 | 已通过 | EXE/APK 均为 0.10.7，发布器核验版本、哈希、签名及远端单份资源 |
| 本机和原笔记本自动更新 | 已通过 | 两台真实接口均返回 0.10.7；原设备 ID、名称、地址、端口和加密密钥逐项一致 |
| 本机应用再次退出后启动 | 已通过 | 两台设备仍在，主/备副本一致，本机及笔记本官方 owner 均连接；桌面 EXE 哈希与发布文件一致 |
| 实际断电 / 再次系统死机 | 未测试 | 未故意制造系统崩溃；不能保证硬件损坏或两份文件同时丢失时恢复 |

```powershell
node --test test/agent-storage.test.mjs test/agents.test.mjs
node scripts/verify-agent-storage-ui.mjs
python scripts/build-release.py
python scripts/build_android_test.py
adb -s emulator-5580 shell am instrument -w -e stage device-storage com.anso.remotecodex.tests/.Probe
```

浏览器脚本需要现有 Playwright 安装（可由 `REMOTE_BRIDGE_PLAYWRIGHT` 指向模块）。测试只创建隔离目录及合成设备，不向官方任务发送消息。Android 测试添加并清理合成设备，保留模拟器已有设备，禁止将用户物理手机当成默认测试目标。原始测试输出位于被忽略的 `work/device-recovery/`。

## 0.10.24 连接诊断与数据保护

2026-09-10：连接诊断、旧设备草稿隔离、Android 带校验独立历史设备存储、通知未知状态、官方统计公平扫描和关键配置保护见 [CONNECTION-DIAGNOSTICS.md](CONNECTION-DIAGNOSTICS.md)。旧文档中的 SharedPreferences 可变设备存储由本版迁移替代；原文件仅作迁移输入，Keystore 与加密密钥保持。统计仍只服从官方，无自有已读/运行缓存。关键配置保护中，损坏请求记录不得重放，双副本不一致不得静默覆盖。用户不执行 APK、自行更新验证的安排继续。
