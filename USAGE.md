# 账户额度显示（0.10.9）

## 2026-09-17：目标设备本地额度历史

侧栏当前额度旁增加图表按钮，设备菜单的额度标题旁也有“历史”入口。Windows/Android 使用同一界面，可选择目标设备和最近 **1 天、7 天、30 天**，默认 7 天；额度窗口优先选普通 Codex 周额度，只有有记录的窗口才出现在选择器中。图表显示剩余百分比、最后记录/区间最低/最高，可触摸或移动鼠标查看采样点，也可用滑条/方向键查看精确时间。30 天密集数据使用单路径而非大量点节点。

- **目标设备负责记录**：Windows Remote Codex 服务启动后，在已连接官方桌面时立即读取、此后约每 5 分钟读取一次已有 get_usage_limits。原界面的实时额度刷新也可更新本时间段的采样。每个额度窗口每 5 分钟最多保存一条，保留该时间段最后一次实际观测的数值和时间；不补造启动前、离线、失败或未知值。切换连接、窗口缺失恢复、超过 10 分钟的缺测及不同重置周期不连线。
- **本地保存最近一年**：独立文件 data/usage-history.sqlite，滚动保留最近 365 天。在采样、历史读取和定时维护时删除更早记录；应用停止期间不执行采样/清理，重启后继续维护。SQLite 事务将清理和写入一起提交，FULL 同步与回滚日志防止中断留下半份记录；多进程事务协调，旧观察不能覆盖新观察，secure_delete 清理删除内容。损坏或未知新版结构报错并保留原文件，不用空库替换。开发/实际客户端使用各自已有数据目录。
- **按需从目标拉取**：GET /api/usage/history?days=1|7|30，返回 schemaVersion=1 的目标端历史投影。沿用现有访问密钥、CSRF 和转发名单，仅允许 GET；Windows 与 Android 转发均已接入。打开、切换范围/设备及点“重新拉取”才查询；关闭后释放页面内存，不在控制端复制持久历史。迟到的旧设备/旧范围请求不能覆盖当前内容。
- **明确来源**：只保存官方额度的采样时间、窗口、剩余比例、重置时间和显示名称，不保存账号 ID、credits、令牌、任务内容，也不用历史代替当前额度、已读或运行状态。数据代表所选设备在采样时登录账号的额度；不同设备不合并。只有单点就显示单点，没有记录就显示空状态。历史是观测曲线，不是完整消费账单。
- **断线与旧端**：官方桌面断开但目标 Remote Codex 仍在线时，可以读取保存过的历史；目标 Remote Codex 离线则提示失败并允许重试。旧目标未提供接口会明确提示升级。关闭窗口到托盘仍可采样，退出进程/关机期间没有新记录。新功能需目标 Windows 接入端与 Windows/Android 控制端都更新。

实现为 src/usage-history.mjs、src/usage-recorder.mjs、public/usage-history-view.mjs。独立历史存储失败不阻止实时额度或任务操作；历史弹窗遮住正文时不产生后台已读回执。新增 node:sqlite 使用项目固定的 Node 22.19.0 运行时，无新增下载依赖，不变更官方接口名单或 settings v2 适配。

验证：Node 22.19.0 全套 319 项；新 test/usage-history.test.mjs 检查范围/一年清理、窗口去重、未知/零值、缺失窗口、重启、并行实例、强制结束未提交事务、损坏/新版库保留、后台采样生命周期、存储失败隔离及鉴权 HTTP。共享 UI 1300px/390px 共 8 组，覆盖范围、窗口、空/单点/8640 点密集数据、切设备迟到响应、旧端/失败重试、关闭清理和两个入口；截图已审阅，另通过原额度、已读、侧栏和 Chat UI。Android 24 份生产 Java 用 API35 编译，实际 LocalServer.allowed 在主机 JVM 上验证 GET 历史及拒绝写入。旧源码→当前源码的隔离设备保存/强制重启/删除和 Windows DPAPI 检查通过，现有客户端设备/接入文件字节不变。

本机官方 Windows x64 26.908.9136.0 只读实测：独立目标服务器从官方读取真实周额度，历史 API 与原数值一致；重启独立服务器并保持官方连接关闭后，原历史仍完整可读。只保存本地证据，不将实际用量提交 Git，没有任务写入或真实客户端升级。

本轮版本保持 0.10.39，只开发源码和回归；未打包、发布、安装或执行 APK/模拟器。既有 .39 安装包不包含本次功能。下方版本与发布方式为历史记录，打包以 AGENTS.md 当前 GitHub-only/显式授权流程为准。

## 0.10.22：重置倒计时

重置日期后增加“剩余 N天N小时N分N秒”，每秒按当前时间重算，回到前台立即校正；不靠累计减秒推算。到期显示“已到重置时间，等待刷新”，不伪造已重置额度。无有效时间、断线或未知数据不显示倒计时。共享生产界面验证跨天、到期、无效值、设备切换及手机菜单边界；Windows/Android 共用代码，本轮按用户安排未执行 APK。

左下角只展示所选电脑上普通 Codex 的主额度：存在 5 小时窗口时，显示 `5h 额度剩余 …%`；没有 5 小时窗口时，显示周额度。点开设备菜单，可看各窗口剩余比例和各自的重置时间，普通 Codex 排最前，Spark 等独立模型额度放在后面。Plus/Pro 标识显示在详情的账号共享额度说明中。

以当前官方返回为准，不按订阅名称补造或删除窗口。当前实测本机 Pro 的 `codex.primary` 是 10080 分钟周窗口、没有普通 Codex 5h 窗口；Spark 有自己的 300 分钟窗口，不能占用左下角主显示。Plus 的普通 Codex 5h 窗口是本次优先展示的对象。

## 协议与兼容

- `/usage` → 已运行官方桌面的 `get_usage_limits`。优先读取 `rateLimitsByLimitId`，仅在缺少有效分桶结构时使用 `rateLimits`。
- `windowDurationMins=300` 为 5h，`10080` 为周；不根据 primary/secondary 位置或重置时间推测窗口。
- `schemaVersion=2` 增加 `fiveHour` 和公开的 `planType`，保留原 `weekly` 字段兼容旧客户端。不给外部返回账号 ID、credits、重置额度券或完整原始响应。
- 剩余比例为 `100-usedPercent`，限制在 0–100。`null`、缺失、字符串或非有限数字显示未知，不变为 100%。5h 未知时不偷偷用周额度顶替。
- 新客户端连接旧目标时，仍展示已知周额度，并说明需更新目标后才能读取 5h；不能据此推断账号没有 5h。
- 设备切换保留请求代次检查，迟到的旧设备数据不会覆盖新设备。断线、过期、读取失败清除旧百分比；不将 Spark 当普通 Codex 的替代值。

## 复测

```powershell
node --test test/usage.test.mjs
node scripts/verify-usage-ui.mjs
python scripts/build-release.py
python scripts/build_android_test.py
adb -s emulator-5580 install -r dist/RemoteCodex.apk
adb -s emulator-5580 install -r work/RemoteCodex-tests.apk
adb -s emulator-5580 shell am instrument -w -e stage usage com.anso.remotecodex.tests/.Probe
```

浏览器测试使用 `REMOTE_BRIDGE_PLAYWRIGHT` 指定模块路径。模拟器测试使用独立测试数据和界面节点，不发送真实任务消息、不修改设备账户。发布后需在本机与已授权远端读取真实 `/usage`，分别核对主窗口与副窗口，记录脱敏结果。

## 验证记录（2026-09-09）

- 已通过：Node 99/99，包含 Plus 5h 优先、Pro 普通 Codex 周额度、Spark 不替代主显示、窗口位置变化、零值、未知值、断线和旧协议兼容。
- 已通过：生产界面浏览器测试，验证普通 Codex 5h/周排序、Spark 明确标注、各自的重置时间、切换设备后的迟到响应隔离，以及手机菜单边界；项目选择回归通过。
- 已通过：API 35 Android 隔离模拟器的 `stage=usage`，10 项原生 WebView 检查；未安装到物理手机。
- 已通过：Windows 单 EXE 在 PATH 仅含 System32 时完成内置运行库与官方桌面只读自检。
- 已通过：同步构建并签名发布 0.10.9 APK/EXE，更新服务器每个平台保留一份固定名称资源。
- 已通过：本机与笔记本均已自动升级到 0.10.9，官方连接正常，两个已保存设备均保留。真实本机返回 `planType=pro`，普通 `codex` 只有 10080 分钟窗口，主显示周额度；笔记本返回 `planType=plus`，普通 `codex` 同时有 300 与 10080 分钟窗口，主显示 5h。Spark 未替代主显示。脱敏验证及当时数值保存在忽略目录 `work/usage-deployed-result.json`，不将具体账户用量提交 Git。
