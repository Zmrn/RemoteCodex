# Windows 其他设备通知（0.10.27）

用户要求本机任务继续由官方应用提醒，Remote Codex 只提醒其他电脑。Windows 托盘启动后默认监听全部已保存远程设备，与当前设备/模式/页面选择无关。关闭主窗口隐藏到托盘后继续监听；托盘退出或电脑关机后停止。托盘右键可暂停通知、查看可用设备数、恢复未发出的通知回复。

## 提醒与操作

- 官方已完成、失败、正在等待回答或审批时，在屏幕右下角显示深色原生卡片，包含设备、任务标题和最多 180 字摘要。异步提问在任务继续运行时也可以提醒。
- 点击“打开任务”进入 Remote Codex 中的原设备/模式/任务，不调用目标电脑的官方窗口导航。正文正在前台查看的同一任务不再弹出。卡片不会抢走正在输入的窗口焦点。
- 完成/失败卡片可输入普通文字回复；问题和审批只提供打开任务入口，不自动代选或审批。正式发送仍走原目标的官方任务提交和去重流程。
- 输入约 500 毫秒后保存到独立本地草稿；关闭保留，清空删除，托盘菜单可恢复。已发出但结果未知的回复保留原文并禁用重复发送，用户打开任务核对后可显式删除此草稿。编辑设备地址/密钥后，旧通知草稿不能被转发到新目标。
- 没有输入的卡片约 20 秒后消失；最多同时显示 3 条，按可用屏幕高度减少。展示、关闭、删除通知草稿均不发送官方已读/回答回执；用户打开并实际阅读后仍执行现有官方已读流程。

这里使用 WinForms 通知卡片，未注册 Windows 通知中心；不承诺系统勿扰/通知历史行为。可通过托盘开关暂停。

## 官方数据与连接

两台电脑都需要 Remote Codex 0.10.27 或更新版本。新接口 `/notification-state` 使用独立、短时官方 owner 订阅，校验连接身份、host、真实任务及 owner；只投影当前 runtime 与最新轮次/问题事件，没有读回执。Chat 历史里的合成 completed 不参与通知。

扫描覆盖官方最近 50 条未固定任务与全部固定任务中的本机 Codex owner，10 秒预算、并发 3 项、跨次轮换；扫描成功后约 5 秒再查，网络错误每设备独立退避到 30 秒。旧版本/未支持 owner 明确不可用；设备删除、配置变化和迟到返回会丢弃旧结果。既排除 local 条目，也排除本机网络地址和相同桥接 instanceId，避免把自连别名当另一台电脑。

首次观察已有历史只建立基线，不集中弹旧任务；已观察过的任务在断线后出现新事件可以提醒。仅持久化事件哈希作防重复投递，最多 10000 项任务、每项 128 个事件；它不定义未读、运行或已回答。草稿、通知偏好独立保存在 `desktop-notifications.json`，使用现有校验保护副本与原子保存；不写设备配置、其他草稿或官方存储。未知/超大快照、短暂出现又结束的中间请求和超出官方列表范围的任务不能保证提醒，不能承诺实时或全量捕获。

快捷回复先在目标端重新读取同一 idle 任务及事件标识，再由现有 nativeSend 核对当前 idle 状态；已变化时不发送。官方没有跨这两次读取与提交的原子锁，不能承诺与其他控制端同时操作的绝对顺序。连接异常不自动重发写入，只有连接检查/恢复及只读扫描可自动重试。

## 验证与范围

复测入口：`test/notifications.test.mjs`、`scripts/verify-desktop-notifications.py`、`scripts/verify-widget-ui.mjs`、`scripts/verify-notifications-live.mjs`。原生卡片与隐藏主窗口计时器用隔离假服务验证，不向私人任务发测试消息。只读现场检查只记录汇总，不输出任务标题、正文、设备密钥。

基础支持仍是 Windows x64 官方 OpenAI.Codex 26.901.6511.0、26.903.8094.0 的 Codex 核心。通知 owner 字段沿用已验证的 **26.903.8094.0** 适配；不扩展到 26.901、Chat 或 Work。Chat 仍为列表/文字历史与实验性续写，新建/模型/生成图片未完成；Work 没有独立验证。VS Code 共享 broker 沿用原组合的核心证据，本轮不宣称新通知已在该组合真实验证。

Android 同步版本和共享导航代码；本轮未增加 Android 任务通知、未安装/运行 APK，按用户安排由用户更新验证。现有 Android 小组件与后台统计保持。

本轮源码回归 194 项 Node、11 项 Windows 原生通知与隐藏主窗口检查、13 项窗口持久化检查通过；共享 UI 验证 Windows/Android 深链、任务草稿保留、官方未读与侧栏 9 组导航场景。官方 26.903.8094.0 只读扫描 10 条候选，3 条 owner 快照可确认，结果明确为部分覆盖；没有向真实任务写消息或回执，没有跨两台真实电脑触发通知/快捷回复实测。最终包、线上及升级验收结果另行记录。

## 发布与安装结果

2026-09-10，功能提交 `1135cdb` 已双端发布。本机通过内置更新器 0.10.26 → 0.10.27，原有 2 项设备、选择、加密密钥、接入/更新设置、独立历史、bridge-state 草稿与请求记录及旧回执保留；官方 PID 108796 未变并恢复连接。安装版原生通知监听已启动，当前 1 台远程设备明确为需要更新，本机不在监听目标中。

| 资源 | 字节 | SHA-256 |
| --- | ---: | --- |
| RemoteCodex.exe | 44114432 | `ce40f882631d41dce600c26ff7d6be89de1a4ed5138993e9f0cde0fe3da191df` |
| RemoteCodex.apk | 253381 | `28152cf861d44980b3398c646ab7e204be45a83ff659e032ffaf5a3ef4fb4a20` |

兼容清单 `b7f6e459f1045b855f1a8c48df6ed66984ebbecf1fa83c1f740fae3f544eb7a5`；Android 原证书 `3c0a98ec3c9f37318525f5d0e4afb3417812215d625e48a9013b5ee649acb2b1` 保持。线上双清单签名、完整包哈希与固定发布说明一致。最终包内自检及旧版→最终包强制重启/删除检查通过；最终运行时标识 `0.10.27-723fe100147b`，不要使用较早同版本中间包。未执行 APK；没有两台真实电脑之间的通知触发/快捷回复实测。
