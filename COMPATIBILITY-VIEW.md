# 官方接口兼容性页面

已随 0.10.31 正式发布，构建与包校验见 GITHUB-UPDATES.md；不扩大官方兼容版本或真实行为验证范围。

入口为帮助（问号）→ 官方接口兼容性。Windows 和 Android 使用同一个页面，可选择任意已保存电脑；筛选设备只影响这份报告，不改变主界面设备或任务。

## 接口检查名单

直接复用 `src/official-desktop.json` 的 `tools` 与 `ipc`，当前共 9 项 app-tools、11 项 IPC（含连接握手与网站授权响应）。这里是 Remote 实际使用的指令及所用参数摘要，不是官方全部功能表。名单外的新增、删除或修改不参与异常判断；新增 Remote 功能时，维护中央清单即可。

页面显示目标 Remote 版本、接口清单短哈希、正在运行的官方版本、官方最新版与检查时间。每行比较“Remote 使用 / 官方运行版 / 官方最新版”，异常优先，可以只看异常及待验证项。

- 红色：已取得目录但缺少指令、缺少所用顶层参数，或包内声明的协议版本不同。
- 黄色待验证：尚未下载/运行新版、连接失败、无法读取或识别协议表。不能因为没有取得数据就认定接口被删除。
- 声明一致：当前可比较项相同；不等同于执行该操作成功。

版本支持名单和官方已读支持名单单独提示，不能把“未加入支持版本”误显示成某条指令已确认不兼容。报告不会自动修改版本保护。

## 数据从哪里来

| 数据 | 来源 | 是否需要新版运行 |
| --- | --- | --- |
| 官方最新版号 | `updateDiscovery.manifestUrl` 指向的官方公开 Windows 更新清单 | 否，无需下载安装包 |
| 指令与所用顶层参数 | 已运行官方应用的实时 `tools/list` | 是；不同版本不能借用当前目录 |
| IPC 协议版本 | 当前包或已下载新版 `app.asar` 内的声明 | 否，但必须有该版本的本地包 |
| 连接握手 | 当前独立连接的握手结果 | 是 |

目前没有可直接按 Windows 包版本查询这些内部接口的完整公开 schema 文档；公开 app-server/CLI 文档不能代替桌面内部管道。新版本尚未运行时，即使包内 IPC 相同，工具参数也保持待验证。官方清单读取失败不影响已取得的运行版结果。

新版包只从已确认官方进程所属 WindowsApps 目录定位同一产品、架构及发布者的指定版本；客户端不能提交任意本地路径。检查不下载完整应用、不启动或升级官方软件。

## 实现与状态边界

`GET /api/compatibility/report` 返回 `schemaVersion=1`、`checkScope=used-interfaces-only`。同一目标端合并在途读取，结束即释放，不持久保存报告或官方状态；点击重新检查会重新采集。`src/compatibility-report.mjs` 使用自己的 Desktop 连接，只做发现、工具目录和握手，不调用任务工具，不跟随任务、不改变主查看订阅。诊断连接无需任务上下文，且允许在 `list_threads` 缺失时取得其他官方工具用于报告异常；正常业务连接仍保留原要求和身份检查。

Windows 转发及 Android `LocalServer.allowed` 仅新增此 GET，未添加新的写入权限。页面关闭或切设备时取消渲染旧请求，迟到结果不能覆盖新设备。旧端不支持报告时提示更新目标 Remote。

复制报告采用固定字段投影，包含接口名称、参数、协议号、版本和原因，不含设备地址/名称、密钥、安装路径或任务内容；剪贴板不可用时提供手动复制框。

检查仅覆盖存在性、顶层所用参数和协议版本，不校验全部字段类型、返回内容、事件语义或执行效果。接口描述方式本身无法识别时显示未知。需要修复时以标出的具体接口为范围，不检查 Remote 未使用的官方功能。

## 2026-09-10 验证

- 227 项 Node 回归通过，包括名单外变动忽略、缺工具/参数/版本变化、未知证据、旧版目录不冒充新版、更新清单校验、独立连接释放、HTTP 模块与只读权限。
- 共享 UI 7 组检查通过：帮助入口、三版对照、异常排序/筛选、1300/390 宽度、复制及回退、设备切换迟到结果、旧端/断线、关闭和只读请求。原桌面 UI 10 组通过。
- 23 个 Android 生产 Java 源文件以 API 35 编译，R/BuildInfo 为隔离桩；主机 JVM 直接检查生产 `LocalServer.allowed` 的 GET 通过、POST 和 Probe 拒绝。不是 APK 或手机行为测试。
- 本机真实只读报告：运行版 26.903.8094.0 的 19 项检查一致；公开最新版为 26.903.9818.0，已下载包的 9 项 IPC 声明一致，9 个工具及握手需新版运行后核对。任务写入 0 次。

只读证据保存在本轮工作区 `work/live-compatibility-report.json`，UI 复测入口为 `node scripts/verify-compatibility-ui.mjs`，报告逻辑为 `test/compatibility-report.test.mjs`。隔离 UI 不是已发布安装版。未构建、发布、安装或执行 APK，未重启官方应用，未将新版加入支持名单。
