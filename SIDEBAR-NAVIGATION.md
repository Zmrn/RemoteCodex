# 侧边栏导航 · 0.10.20

用户报告手机上切换Codex/Chat或设备后，整个侧边栏自动收起。0.10.19的switchAgent直接调用closeDrawer，模式切换复用该流程；恢复上次任务的selectThread又会收起一次。仅移除第一处不足以修复带任务恢复的模式切换。

现在设备/模式切换只关闭对应菜单，保持侧栏当前状态。自动恢复任务显式保留侧栏；不在异步完成后强制重新打开，因此用户在等待连接时主动关闭也有效。主动选择任务、新建、前进/后退任务导航、小组件跳转仍显式进入正文并收起侧栏。初次启动默认收起，横屏/桌面布局规则不变。

手机侧栏打开时正文处于inert状态；已读判断在计时开始和提交前都检查该状态，避免自动恢复的回复被侧栏遮住却记为已读。关闭侧栏后重新按可见区域、滚动位置与前台停留时间判断。没有修改设备存储、访问密钥、回执格式或官方已读状态；官方应用内已读尚未同步的问题仍独立存在。

## 回归

`node scripts/verify-sidebar-navigation-ui.mjs`使用生产界面与隔离HTTP接口，模拟Android平台，390×844竖屏、844×390横屏。旧版在第一步切换模式后确实错误收起；修复后9组检查通过：

- 无历史任务及自动恢复已有任务时切换模式，侧栏保持展开，只收起选择菜单。
- Chat/Codex草稿和不同设备草稿隔离。
- 切设备、设备失败及恢复连接保留侧栏。
- 读取或连接期间主动关闭，迟到结果不会重新展开侧栏。
- 侧栏遮挡回复时不发已读回执，关闭后真正可见才确认。
- 手动选任务、新建、关闭按钮、遮罩、Escape及小组件跳转保留原行为。
- 横屏桌面侧栏保持可见。

前台焦点在隔离浏览器中固定模拟，避免并行浏览器影响定时断言；没有模拟实际Android触摸或系统返回键。原`verify-device-switch-ui.mjs`的8组HTTP/SSE重连、慢读取取消和错误恢复检查，以及`verify-widget-ui.mjs`的1300/390px已读与跳转/草稿检查通过，真实官方任务写入0次。

148项Node、13项Windows窗口、兼容清单、双端构建及签名/原Android证书/包元数据和最终EXE包内只读自检通过。按此前用户安排，本轮未安装/执行APK，手机实际交互待用户更新验证；浏览器检查不能当作APK行为验收。

官方支持版本与Codex/Chat/Work范围保持不变，详见`releases/0.10.20.md`与中央兼容清单。

## 正式发布

2026-09-09已双端正式发布0.10.20，线上签名清单、固定发布说明和完整产物哈希核对通过。EXE 44,066,304字节，SHA-256 `4c7e3a879057a5567a0e114b79fcd610695e4e3c2c6cfe7307e370288be53b4c`；APK 236,614字节，SHA-256 `7e04c115bc0c9fa77f8feb364244d26ba68cdd9fa3bb5f9b9ba2d0c8d783e4ee`。兼容清单仍为`1feeda2ca2a4987a7896f5d16f7ad8cdd177cf31e7c41b3510886ead509e34c1`，原Android安装证书保持不变。

笔记本通过内置更新器0.10.19→0.10.20，现有2项设备、选择、密钥、接入/更新设置与独立历史保留，官方连接恢复且进程未重启。未安装或运行APK。
