## 本次更新

- 修复官方回复中的 GIF 被显示为文字和文件卡的问题。GIF 动画可直接在消息和放大预览中播放，支持图片内缩放与拖动。
- GIF 接收和原图保存上限提高到 64 MiB，解决大于原 25 MiB 上限的动画无法查看；其他图片和发送限制保持。
- 本地 GIF 下载保留原文件名和完整动画，沿用加载提示、失败重试及刷新时保持图片稳定的行为。
- 修复 Windows 图片预览读取自身 Blob 被页面策略拦截的问题，放大后可以保留独立原图引用。

**请同时更新目标电脑的 Windows 接入端，以及使用的 Windows／Android 控制端。** 设备、密钥和草稿存储格式保持，更新直接从 GitHub Releases 下载。

## 验证范围

开发阶段使用官方 Windows x64 26.908.9136.0 的真实回复，只读核对 28,482,324 字节 GIF；共享界面的桌面与手机尺寸验证动画播放、预览及原图下载，字节与官方原文件一致。没有修改官方任务或已读状态，私人图片未上传仓库。最终安装包校验和当前官方连接结果见发布记录。

本轮不安装或运行 APK，由用户更新后验证。兼容性按当前实际接口逐功能判断；已知官方会话设置接口 v2 尚未适配，对应修改设置及新建时指定权限依赖不满足时仍不可用。

Chat 列表与文字历史保持，文字续写待专用真实验证；普通 Chat 新建、模型、生成图片及 Work 未完成适配未纳入本版。历史行为验证版本与详细范围由下方中央清单生成。
