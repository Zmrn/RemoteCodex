## 本次更新

- 修复超长 Codex 会话虽然能查看内容，却无法发送消息的问题。发送前改为读取官方当前任务信息并核对任务所有者，避免整轮正文超过官方传输上限。
- 修复 Windows 和 Android 中图片反复切换为“加载中”、导致正文上下抖动的问题。刷新时保留未改变的已加载图片，正文更新和下载地址更新也保持稳定。
- 断线重连后，已加载图片继续显示，未完成的下载可以恢复；加载失败支持手动重试。官方更换或删除的图片仍会同步更新。
- 修复历史校验提前结束时文件句柄未及时释放的问题。发送失败继续保留草稿，结果未知的消息不会自动重复发送。

**请同时更新目标电脑的 Windows 接入端，以及使用的 Windows／Android 控制端。** 发送修复需要目标电脑更新，图片显示修复需要控制端更新。设备、密钥及未发送草稿存储格式保持，更新直接从 GitHub Releases 下载。

## 验证范围

官方 Windows x64 26.903.9818.0 上，实际问题会话通过当前任务信息与新所有者状态的只读核对；没有向该会话发送测试消息。277 项 Node 回归和共享页面桌面／手机宽度的图片、重连、分页及相关功能检查通过。本轮不安装或运行 APK，由用户更新后验证。

当前任务、未读状态和消息仍以官方数据为准，不使用历史文件的完成标志授权发送。官方列表缺少目标且单任务接口仍超限时，会明确提示状态无法确认、消息未发送。

Chat 列表与文字历史保持；普通 Chat 新建、模型选择、生成图片及 Work 的未完成适配未纳入。兼容性按实际功能依赖判断，详细范围由下方中央清单生成。
