## 本次更新

- 图片下载和解码时显示加载占位；失败后可以重试，正文、Markdown 图片和放大预览共用处理。
- Codex 任务运行中，Ctrl+Enter 将消息直接发送为“调整方向”；普通发送继续进入队列。发送失败保留文字和全部图片，未知结果不自动重发。
- 修复已打开回复却未向官方标记已读的问题：正文和已读校验使用同一份官方实时内容，官方历史接口返回空内容时也能正确校验。最新回复末尾可见后请求官方清除标记，不需要滚完后面的文件/命令记录。
- 仅明确尚未发送的已读校验失败允许有限重试；发送结果未知不重放。蓝点和小组件计数仍以官方状态为准，不清理或重建用户数据。

## 更新与验证范围

Windows EXE 和 Android APK 由同一次 GitHub Actions 构建，沿用原更新签名和 Android 安装证书；更新直接从 GitHub Releases 下载。两端及目标电脑桥接器均需更新，才能使用全部修复。0.10.28 及更早版本首次迁移仍需从 GitHub 手动覆盖安装，保留原数据，不要卸载清理。

本次源码已通过 Node 回归、图片/快捷键/已读/侧栏共享界面检查；已读完整 HTTP 与隔离官方 IPC 回归已纳入 GitHub Checks。实际问题任务仅在官方 Windows x64 26.903.8094.0 进行只读核对；本次新增交互没有真实任务消息、调整方向或已读写入验收，也不包含 APK 运行测试。未完成的 Chat 修改没有混入本次发布。下方兼容范围沿用已有验证，不因本次构建扩大；官方已读同步与通知仍限于已验证的 26.903.8094.0 本机 Codex，Chat/Work 不使用该入口。
