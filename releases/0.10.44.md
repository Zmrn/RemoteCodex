本版修复图片/GIF中断后从头下载的问题：有可靠校验信息的已接收片段会保留，重试从断点继续；75秒限制改为无数据闲置超时，持续传输不会因总时长超过75秒中止。原图变化时重新下载，最终校验完整SHA256。断点仅在当前页面内存保留，最多96MiB、30分钟，重启应用后需重新下载。

选择未加载的Codex任务时，自动调用官方已有的打开指令，并等待当前所有者确认加载完成。加载期间可编辑文字和图片，失败可点“重试加载”，不会发送占位消息或自动重发草稿。该操作会切换目标电脑官方应用当前显示的任务；不批量激活其他任务。带图和设置发送共用加载准备，保留既有提交保护。

请同时更新目标电脑的Windows接入端与使用中的Windows/Android控制端，无需重新配对。官方Windows x64 **26.915.4065.0** 已通过专用任务真实HTTP冷加载验证（notLoaded→resumed/idle、无消息写入），21/21接口及25/25功能依赖匹配；这不代表全部功能行为均已重测。已有Codex能力继续按当前官方接口启用。Chat只包含已完成的列表/阅读和受能力约束的文字续写；普通Chat新建、图片及未完成Work适配未纳入。未运行APK/模拟器、升级现有客户端、重启官方应用或复测断开RDP时的冷加载。
