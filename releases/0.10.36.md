## 本次更新

- 修复官方 Codex 自行创建的任务未出现在列表中的问题。实时列表遗漏时，从当前官方数据库和官方项目归属只读补齐任务名称及归属；不保存另一份任务目录，不根据磁盘记录推断未读或运行状态。
- 修复长会话可读但蓝点迟迟不消失的问题。分段中的最新完整回复与官方实时状态核对后即可同步已读；通知前使用任务元数据和新 owner 状态，避免再次传输整个超长回合。翻页与头部刷新保持有效的可见阅读凭据，蓝点仍由官方确认。
- 官方回复中的创建任务指令显示为可点击卡片，直接打开同一设备上的对应任务；尚在创建的任务显示等待状态，代码示例保持原文。

**请同时更新目标电脑的 Windows 接入端，以及使用的 Windows／Android 控制端。** 设备、密钥和草稿存储格式保持，更新直接从 GitHub Releases 下载。

## 验证范围

开发阶段已在官方 Windows x64 **26.903.9818.0** 只读复查漏列任务；完整已读通知流程通过隔离官方替身和共享界面回归，未修改用户真实任务。Node 22.19.0 全套290项、桌面与手机宽度下的新任务/分页已读界面、原列表/已读及图片稳定性回归通过。最终安装包的验证记录见本版发布记录。

仍需最新回复末尾在前台真实可见至少800ms；状态未知、owner不可用或通知结果不确定时，不冒充已读成功、不自动重放。补齐仅适用于官方归属可确认的未归档普通任务，最近列表仍最多50条。任务元数据也无法通过实时接口确认时，不能用磁盘记录授权发送或已读。

本轮不安装或运行 APK，由用户更新后验证。兼容性继续按当前实际接口逐功能判断，未知版本号不整体禁用。历史行为验证版本与详细范围由下方中央清单生成。

Chat列表与文字历史保持，文字续写待专用真实验证；普通Chat新建、模型、生成图片及Work未完成适配未纳入本版。
