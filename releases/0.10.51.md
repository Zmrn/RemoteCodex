本版适配官方 Codex Windows 桌面端 26.924.2738.0 的 app-tools 调用封套变化。此前 Remote Codex 虽能列出接口，却在读取项目、会话和内容时收到“Invalid app tool request”；现在补齐官方新增的 `callerSource` 字段，恢复这些只读调用。连接和兼容性诊断还会实际进行一次只读工具调用，避免仅凭接口目录匹配就显示功能正常。调用封套异常只影响依赖该工具的功能，所有者 IPC 按自身连接独立判断。

请同时更新目标电脑的完整版 Windows 接入端，以及使用中的完整版或 Limit 版 Windows／Android 控制端。四个安装文件同次构建、同版发布；两版保持独立安装与设备连接。

已在升级后的官方桌面端只读验证项目、任务列表、额度、Codex 与 Chat 历史，以及此前报错的长会话首段分页读取。官方任务写入、已读、排队和审批行为未在本版对真实用户任务验证，因此不将 26.924.2738.0 加入完整行为已验证名单。普通 Chat 新建／图片和未完成 Work 功能仍不在本版范围。发布过程中不执行 APK／模拟器，也不升级现有客户端。
