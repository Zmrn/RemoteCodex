## 本次更新

- 普通新建会话由官方 Codex 自动命名，不再使用测试标题。已有名称可在会话行右键重命名，手机共享界面支持长按；改名通过官方接口同步，Chat 暂需在官方应用操作。
- 创建返回会话 ID 后立即打开内容，列表不再等待项目读取；前台每 5 秒刷新并防止旧响应覆盖。官方索引暂未包含新会话时显示同步说明，仍以官方列表为准。
- 更新页区分当前安装版本、最近验签的远端最新版、下载目标及已校验安装包版本，显示检查时间。
- 帮助页增加“官方接口兼容性”，只核对 Remote Codex 使用的接口名单；缺失或不兼容标红，没有证据的项目显示待验证。
- 支持官方 Browser 网站访问申请：拒绝、一次及请求明确声明的会话/此网站授权；全网站授权转官方应用处理。

两端和目标电脑的 Remote Codex 均需更新才能使用全部功能。设备、密钥、未发送草稿存储格式保持；更新仍直接使用 GitHub Releases。0.10.28 及更早版本需要手动覆盖安装一次迁移，请保留数据。

## 验证范围

本版包含已通过本地与 GitHub 隔离回归的修复。网站授权仅适配官方 26.903.8094.0；兼容性页面不自动放行新官方版本。改名回执不代替官方列表，5 秒刷新不保证官方索引在 5 秒内同步；带图新建沿用原有准备轮流程，自动标题可能受其影响。

本次未进行真实会话创建/改名或网站授权往返，未安装或执行 APK、未升级现有客户端。未完成的 Chat 图片/普通新建改动未纳入。Chat/Work 与官方版本的具体支持范围如下。
