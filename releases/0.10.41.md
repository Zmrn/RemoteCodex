# Remote Codex 0.10.41

修复部分图片点击重试后立即失败，并新增图片下载进度。

- 正确读取官方消息中 `/C:/…` 形式的 Windows 图片及文件路径，避免被解析为重复盘符而报文件不存在。
- 会话附件图片显示下载百分比、已下载大小和总大小；总大小未知时显示实际接收量。
- 加载失败显示具体原因，包括文件缺失、权限不足、HTTP 错误、超时、连接中断和图片无法解码。
- 失败后显式重试会重新下载；刷新继续保留正在下载的请求和已加载图片。
- Android 转发保留图片大小信息，与 Windows 共用进度界面。

请同时更新目标 Windows 接入端和使用中的 Windows/Android 控制端。第三方 HTTPS 图片仍由浏览器直接读取，数值进度用于可读取响应字节的会话附件图片。

最近官方 Windows x64 26.908.9136.0 已完成问题图片的只读读取与解码验证，不代表所有写操作实测。设置 v2 仍待适配，修改会话设置和新建指定权限暂不可用。Chat 普通新建、图片发送及未完成的 Work 适配未纳入。

APK 与 EXE 由同一次 GitHub Actions 构建，沿用原签名和 GitHub 直接更新渠道。
