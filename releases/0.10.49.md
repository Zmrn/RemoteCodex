本版修复 Limit Remote Codex 发送消息后，回复已经到达目标电脑，但当前会话一直停在“正在处理”，必须退出再进入才能看到回复的问题。目标端现在只向受限配对发送其所属会话的最小实时状态通知，当前会话会据此自动刷新；其他配对的会话事件、官方连接详情不会进入该事件流。配对码更换或撤销时，已有实时连接立即关闭。

请同时更新目标电脑的完整版 Windows 接入端和正在使用的 Limit 版 Windows／Android 控制端。四个安装文件同次构建、同版发布；两版仍独立安装并保存各自的设备连接。Limit 版只能查看和操作同一配对码创建的无项目会话，不能访问本机官方应用或目标电脑的项目列表。

隔离的两跳 HTTP／事件流回归覆盖发送后刷新、不同配对身份的事件隔离及换码撤销；共享界面回归覆盖桌面和手机尺寸的回复自动显示。官方 Windows 应用只做只读连接与列表核对，不把接口匹配等同于全部行为实测。普通 Chat 新建、图片和未完成 Work 功能未纳入本版；发布过程不执行 APK、不升级现有客户端，也不向真实任务发送测试消息。
