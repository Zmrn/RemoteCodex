## 本次更新

- 修复超长 Codex 会话在官方窗口可读、Remote Codex 却整页加载失败的问题。官方读取接口连一轮历史也无法返回时，从当前官方应用的原始历史文件只读分段加载，先显示可读内容。
- 文字按消息分页，图片通过独立请求加载，支持加载中提示、失败和手动重试。官方历史发生变化或连接重建时，重新取得内容与图片引用。
- 读取失败保留已显示的内容和未发送草稿。实时状态、运行操作及未读标记继续使用官方数据，不从历史文件推断当前状态。

**请同时更新目标电脑的 Windows 接入端，以及使用的 Windows／Android 控制端。** 历史读取修复需要目标电脑更新；只更新手机无法修复旧接入端的读取失败。设备、密钥和未发送草稿存储格式保持，更新直接从 GitHub Releases 下载。

## 验证范围

实际问题会话在官方 Windows x64 26.903.9818.0 上完成只读验证：约535 MiB的原始历史分为32页，显示820项消息与记录，无重复，并成功单独加载图片。测试没有修改官方历史或发送消息。

此降级适用于当前官方列表可确认的本机 Codex 任务。发送、停止和已读同步仍依赖官方实时接口，超限任务的这些操作可能继续受到官方接口限制；不把历史可读等同于全部操作恢复。

Chat 列表和文字历史保持；普通 Chat 新建、模型选择、生成图片及 Work 的未完成适配未纳入。本轮不安装或运行 APK、不升级现有客户端。接口兼容性继续按实际功能依赖判断，详细范围由下方中央清单生成。
