重新排布 Windows 远程通知：设备与状态、任务标题、摘要和操作分层显示，统一字体、间距与圆角按钮；回复框点击后展开，长标题和高缩放不挤压操作区域。

通知改为 5 秒无操作自动收起。输入/点击重新计时，停输 5 秒后先保存草稿再关闭；清空输入或已有草稿都不会让卡片永久停留。发送中等待结果，保存失败保留文字；可从托盘恢复草稿。修复关闭回执失败后轮询重新弹出同一通知的问题，不改变官方已读状态。

仍只提醒其他设备。目标端 0.10.27 即可提供通知接口，本次需更新的是 Windows 控制端。通知仅支持官方 Windows x64 26.903.8094.0 本机 Codex owner；Chat/Work 通知尚未支持，使用原生浮动卡片而非 Windows 通知中心。Android 同步构建、签名和发布，未安装/执行 APK，由用户更新验证。
