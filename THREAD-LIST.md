# 会话命名、重命名和列表同步

## 2026-09-12：官方创建任务漏列及创建卡片

真实问题任务可以由官方read_thread直接读取，当前list_threads只有17条且遗漏该任务。官方state_5.sqlite中任务存在、未归档，旧title为空，新name已有正式标题；官方thread-project-assignments已将其分配到原项目。不是Remote本地创建记录丢失。

每次刷新在原实时工具列表之外，并行读取当前已核对官方进程home的官方SQLite只读事务和全局项目/无项目归属。只补工具缺失且明确属于官方侧栏的未归档普通任务，使用官方name/preview/title、项目ID和更新时间，不猜工作树项目，不扫描消息，不保存另一份任务目录。已有工具行与固定顺序保持，合并最近列表最多50条；候选扫描最多5000行/1.5秒、投影最多50行，坏格式、未知schema、无归属或读取失败不补行，不影响原工具读取。读取期间连接/目录变化则丢弃结果。

索引补行明确标metadataSource=official-local-index，运行状态为unknown；运行/未读仍由新owner观察给出。索引不能授权发送、停止、已读通知或其他写入。删除/归档/移出官方归属在下次成功刷新移除补行，不靠创建回执或本地持久化保留它。现有OFFICIAL-DATA.md的“官方列表”扩展为这两种官方元数据来源，旧“只能等工具列表”描述由本节覆盖。

共享Markdown将独立的`::created-thread{threadId="..."}`渲染成“打开任务”卡片，标题来自当前官方列表；点击进入同设备的精确任务，保存导航草稿沿用原流程。clientThreadId只显示创建中，不冒充已可操作的正式ID。代码块/代码示例保持文字，Chat禁用Codex跳转；链接本身不创建或补入任务。

回归入口：test/thread-sync-read.test.mjs、scripts/verify-thread-sync-read-ui.mjs（纳入Checks）；后者贯穿共享UI、HTTP、官方索引替身、真实分段解析和官方通知替身。Node22.19.0全套290项、新共享UI3组、原已读UI7组、原列表UI8组、图片稳定UI6组及兼容清单检查通过；索引辅助器显式UTF-8，覆盖中文目录、中文标题和emoji。真实只读复查已能列出问题任务，未创建、改名或写入真实任务。版本仍0.10.35，等待明确打包；没有执行APK。

2026-09-10 源码修复，Windows/Android 共用；已随 0.10.31 在 GitHub 构建并正式发布，见 GITHUB-UPDATES.md。未安装或执行 APK，以下专项验证记录仍为源码阶段证据。

## 命名与重命名

普通新建错误地复用了测试创建逻辑：向官方 create_thread 显式传入 RemoteBridge-Probe-时间戳 标题及同名 projectless 目录，并登记为测试任务。现普通 create 不传 title/directoryName，由官方自动命名和安排目录，也不登记为测试目标。只有内部 createProbe 使用测试名并登记；测试脚本、专用权限上下文和固定兼容性测试改为显式调用。生产 HTTP 不能选择 Probe 路径。

旧 bridge-state.json 记录保持，不按名称批量改动任务。已有官方 Probe 标题可由用户重命名，不自动改写。图片新建沿用原有文字准备轮再发送完整图文的流程，也不传强制标题；官方标题生成时机以及准备轮对自动标题的影响由官方处理。

会话行右键 → 重命名，手机共享界面支持长按。操作绑定所点行的设备/真实 ID，支持 1–200 字单行标题。提交前重新确认官方 Codex 任务、原名及连接；原名已变化则提示刷新后重新修改。通过官方 set_thread_title 元数据接口操作，不发送消息、不直接写官方文件。回执必须匹配 threadId/title，但不据此修改列表；后续官方列表提供可见标题。未知结果保留输入并禁止自动重发，相同请求跨重启去重。官方没有标题 compare-and-swap，前置核对不能消除同时修改的全部竞态。

Chat/Work 没有已验证的重命名入口，对应菜单禁用并提示在官方应用处理。

## 列表与官方数据

官方 26.903.8094.0 包内 list_threads 对 Codex 调用 thread/list（useStateDbOnly=true），并等待 Chat 列表等并行来源。索引可能落后于官方窗口。本轮没有回放用户截图当时的分钟级延迟，不能声称已量出或消除官方索引等待。

Remote 原先创建后先等待完整项目/列表/状态刷新再打开真实 ID；项目慢请求阻塞列表显示，刷新与轮询之间也没有列表响应先后保护。现改为：

- 拿到官方创建 ID 就立即读取并打开内容，不等待完整列表或项目刷新。
- 任务列表先显示，项目发现独立完成；较旧列表不能覆盖已显示的新结果，旧连接返回拒绝使用。
- 可见且连接正常时每 5 秒轮询；创建、改名及已订阅任务标题变化触发合并刷新，正在读取时最多安排一次后续刷新，切设备取消旧调度。
- 历史工具中的旧标题不反写列表；已有列表成员的页眉名称以官方列表为准。
- 自己刚创建的 ID 尚未进入官方列表时，仅显示同步说明，不补造会话行。官方返回该 ID 才结束提示，成员与顺序保持官方来源。

没有创建本地会话列表或标题覆盖缓存。官方接口仍限最近50项非固定任务与全部固定任务；5秒是正常前台调度间隔，不是索引一致性或慢网络下的显示时限。

## 验证与范围

- Node22.19.0 全套243项通过；7组新专项覆盖普通/测试创建区分、改名精确参数与回执、冲突/错误输入/Chat拒绝、未知结果重启去重、列表连接更换、生产静态资源/CSRF/POST路由。
- `scripts/verify-thread-list-ui.mjs`：生产页面与静态服务、隔离合成API，8组检查通过。覆盖右键改名、旧历史不回退标题、慢项目不挡列表、列表乱序、立即打开新ID/索引同步说明、5秒调度、未知提交、迟到改名与切设备、Chat禁用、手机长按布局。
- `scripts/verify-project-ui.mjs` 与 `scripts/verify-sidebar-navigation-ui.mjs` 通过，后者9组设备/模式/横竖屏侧栏回归。
- 23个 Android 生产 Java 文件以API35编译通过，主机JVM检查 title 仅允许POST转发；未执行APK，未宣称手机长按已真机验证。
- 官方包源码核对创建可省略title、改名要求正式回执及列表索引来源。本轮没有创建/重命名真实任务，没有读取私人消息替旧任务取名，也没更新官方软件。

首次 Node24 全套有 view-transport 测试子进程异常退出；使用与云包/CI一致的Node22后243项全部通过。独立测试与共享UI通过，失败运行不作验收结论。

支持仍为 Windows x64 官方26.901.6511.0、26.903.8094.0的既有Codex范围；本轮源码只读核对为后者，未真实改名往返。未将26.903.9818.0加入写入名单。set_thread_title原已纳入中央清单，调整用途/消费者/回归后名单仍20项。
