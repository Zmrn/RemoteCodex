# 连接诊断与可靠性修复（0.10.24）

侧栏「连接诊断」、问号菜单和连接状态提供同一个诊断窗口，可选择任一已保存设备。Windows 本机直接检查桥接状态，远程设备分层检查配置、Tailscale 名称解析、端口、HTTP 鉴权、Remote Codex 桥接、官方连接、版本能力和配置保护。超时只报告本次无法确认，不能推定唯一原因。

检测仅请求保存目标的只读状态接口，不发送任务、不修改网络、不重启官方应用。重试连接、编辑设备和检查目标更新由用户显式点击；检测过程中改变目标时丢弃旧响应。「复制脱敏诊断」通过白名单排除设备名称、地址、密钥、任务 ID 和消息。协议入口为控制端 GET /api/diagnostics?agent=<已保存ID>；目标沿用 /bridge/v1/api/status，无新增官方 IPC 方法。旧端缺少能力字段时标为未知或提示更新。

## 本轮修复

- 设备删除或地址改变后，属于旧目标的文字、原图和取回记录进入「保留的草稿」，不会自动落到备用设备的输入框。用户可复制文字，或明确恢复到当前设备空白的 Codex 新对话；不恢复旧任务 ID、模型项目设置或队列写入 ID。旧草稿备份继续保留，需用户显式删除。历史版本尚未记录地址指纹的草稿只能按已有设备 ID 判断；不能追溯升级前已发生的地址变化。
- Android 设备增删改和选择迁移到私有 devices-v2 文件存储。原 SharedPreferences 仅作首次迁移输入，原 Keystore 身份和加密密钥保持。使用进程内锁、系统文件锁、修订号、校验摘要、临时文件同步与原子替换；独立提交历史是权威，镜像写入失败须报告不确定结果，不能恢复已提交的删除。最新历史损坏时停止，保留所有文件，不自动回滚到可能含已删除设备的旧记录。
- 通知栏、桌面组件和汇总页共用未知状态文字，没有官方结果显示「—／官方状态未知」，避免显示为正常的 0/0。
- 官方状态扫描保留轮转位置，避免多次刷新总从第一条开始而饿死尾部任务。只保留调度位置，不能继承上一轮任务旗标；耗尽预算的条目仍为未知。
- Windows 接入、请求/草稿记录和更新设置增加校验保护副本。坏文件不会阻止只读界面启动，也不会被空配置覆盖。两份不可读时暂停相关写入；主文件与已提交副本不一致时保留两份并暂停修改。请求记录恢复后保持只读，不能因恢复副本而重放结果未知的写入。配置保护只处理本应用数据，不访问官方历史数据库。

## 验证与范围

入口：`test/review-reliability.test.mjs`（真实本地 HTTP、故障注入和状态投影）、`scripts/verify-diagnostics-ui.mjs`（共享 UI、合成设备及两张原图）、`scripts/verify-device-connections.py`（主机 JVM）、`scripts/verify-agent-restart.mjs`（隔离 Windows 设备与强制重启）。另运行 Node 全量、兼容清单、窗口、设备切换/重连、小组件、侧栏和草稿回归，最终构建、包内自检及发布记录单独核验。

用户安排本轮不安装或执行 APK，Android 真机的迁移、Keystore、启动器、后台与网络行为由用户更新后验证。主机 JVM 和浏览器测试不代表 APK 实测；没有实际断电或磁盘故障试验。同盘保护记录不能解决整盘或整个应用数据目录丢失。

官方支持范围保持 Windows x64 OpenAI.Codex 26.901.6511.0、26.903.8094.0 的既有 Codex 核心支持；官方未读快照仅验证 26.903.8094.0 本机 Codex、官方独占 broker。Chat 列表/文字历史可读，续写实验性，普通新建、模型和生成图片未完成；Work 未独立验证。VS Code 共存证据仍限原开发设备 26.903.8094.0 + VS Code 1.136.2 + Codex 扩展 26.903.61454，本次新诊断与状态未在那里验证。

## 2026-09-10 正式发布验收

0.10.24 已双端发布并完成笔记本内置更新。170 项 Node、13 项窗口、49 项主机 JVM（12 连接、27 展示、10 存储/状态）、7 组诊断与草稿 UI、13 组取回草稿、9 组侧栏、设备切换/重连/小组件回归通过。最终 EXE 包内运行时、原生 DPAPI/官方只读自检，以及旧包→新包保存/强制重启/删除通过。无真实官方任务写入，未执行 APK。

最终 EXE 44,091,392 字节，SHA-256 `63d785fc5b035718c2cd927ff0971f0dfdd2dca3005da217a45fd3d55c5c3df5`；APK 253,305 字节，SHA-256 `1c5356881cfbc25864d3ebecaf83ca047d0e356dea845fefed82ae95b7a8ff9b`。兼容清单 SHA-256 `f53bfc41a02e92cfe8a68c4ac55f6f5b3eeb34a65b75a1cd239cc23e1eb4391a`，与 0.10.23 的清单 JSON 内容一致（检出换行导致字节摘要不同），未扩大官方支持。Android 原证书保持 `3c0a98ec3c9f37318525f5d0e4afb3417812215d625e48a9013b5ee649acb2b1`。线上签名、完整资源哈希、固定发布说明和单份资源核验通过。

笔记本 0.10.23→0.10.24 更新前后，两项设备内容、选择、加密密钥、接入/更新设置和独立历史保留，旧回执文件未读写，官方 PID 108796 未变。安装后的诊断已只读检查本机及另一台已保存电脑，均确认网络/桥接/官方连接；旧目标没有报告桥接版本时按缺失显示，不能据此宣称对方已更新。新 Android 原生存储迁移、诊断与系统后台行为仍待用户手机验证，支持和未测范围见上文。
