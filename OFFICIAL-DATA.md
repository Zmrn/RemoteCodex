# 官方数据与本地草稿（0.10.26）

2026-09-10 用户要求排查与已读蓝点类似的本地残留，并明确未发送的草稿仍由 Remote Codex 处理。本约定覆盖早期根据创建记录补齐任务列表、根据历史推断当前状态的描述。

## 数据边界

| 内容 | 来源与处理 |
| --- | --- |
| 侧栏任务、标题、项目归属 | 官方列表。创建回执中的真实 ID 可用于立即打开内容，但本地创建记录和直接阅读都不再向列表补行。列表暂缺新任务时等待官方列表更新。 |
| 当前运行、等待状态 | 官方当前 runtime / activeFlags。idle 不被历史完成、失败、中断或旧 requests 覆盖。轮次结果仍可作为历史显示。 |
| 未读、已读 | 沿用 0.10.25 官方 owner 的 hasUnreadTurn。用户可见阅读后的通知仍核对最新回报、idle、owner 和账号；未确认不本地清零，详见 OFFICIAL-READ-STATE.md。 |
| 问题是否已回答 | 官方实际回答消息或官方问题结果。忽略旧持久化 done，不从提交 accepted 推断已回答；当前官方 pending 问题优先于同轮旧结果。 |
| 模型、权限、强度和加速的已应用值 | 官方读取结果。提交回执只提示已提交，菜单和后续刷新读取官方值；已加载任务缺字段显示未确认；加速说明使用官方目录，不按模型名称补造速度倍数。更换模型时不擅自把不兼容的强度替换成 medium。 |
| 当前消息 | 同一轮存在官方 owner items 数组时使用该数组，包含删除后的空数组；未提供该数组时保留官方历史工具内容。分页携带官方完整消息 ID 顺序，新快照删除的消息不能被旧分页重新加入。 |
| 队列、加速档位目录 | 优先官方 owner 队列。磁盘备用队列及模型目录来自已核对官方进程的 CODEX_HOME，不能从 Remote Codex 自己的环境猜测。目录无法确认则不可用。磁盘队列继续明确标为非实时；模型目录有来源与时效。 |
| 未发送文字、图片、回答、预选设置 | 本地用户草稿，按设备和任务隔离保存，不逐字同步到官方。失败、导航、重启不应丢失。 |
| 请求标识、取回恢复记录 | 本地防重复发送与输入保护。不是官方消息、运行状态或已回答状态。问题请求按问题保存 ID，相同回答的显式重试复用它，兼容旧 journal；不自动重放未知写入。 |
| 设备、密钥、接入偏好 | Remote Codex 自身配置，沿用独立历史、系统加密和可靠持久化。 |

必要的分页、图片传输和当前页面观察仍存在。它们有读取时点，不是新的官方事实来源；断线/未知不能据此确认当前状态。尚未重新读取的较早历史仍是当次分页快照；本轮不承诺所有历史随官方任意编辑立即更新。官方列表仍有最近 50 项未固定任务限制。

## 验证

- `npm test`：178 项通过；包括旧结果与当前 idle 冲突、队列目录隔离、官方目录解析中断、创建记录不补行、消息删除与旧游标不能复活。
- `scripts/verify-features-ui.mjs`：旧 done 不覆盖 pending、回答草稿保留、提交回执不等于回答、相同回答复用请求 ID、设置回执不抢先勾选、官方变化更新已打开菜单。
- `scripts/verify-settings-ui.mjs`：使用独立动态端口假 API，已加载任务设置与未发送预选、设备/任务隔离、模型不兼容强度不补造、横竖屏检查。
- 项目、分页、历史失败容错、取回草稿清空、小组件、侧栏导航、渐进队列、停止与 Chat 切换共享 UI 回归。项目脚本补充等待目标设备项目列表完成，避免用旧设备中间状态断言。
- 本机官方 26.903.8094.0 三条任务只读核对 owner/runtime/消息/队列/进程目录，无任务写入；9 项隔离 Windows 目录检查及登录元数据投影检查通过。
- Windows 窗口、包内只读自检、旧包到新包设备保存/强制重启及发布/安装验证结果在会话交付记录。APK 仅构建、签名与元数据验证，没有安装或执行，由用户更新后验证。

支持保持 Windows x64 官方 OpenAI.Codex 26.901.6511.0、26.903.8094.0 的 Codex 核心功能；本次实时只读检查仅后者、官方独占 broker。未读同步仅验证后者本机 Codex。Chat 列表/文字历史可读，续写实验性，新建/模型/生成图片未完成；Work 未独立验证。VS Code 共享 broker 沿用原设备 26.903.8094.0 + VS Code 1.136.2 + 扩展 26.903.61454 的核心证据，本轮没有真实写入复测。未改变官方安装、历史文件、账号或签名身份。
