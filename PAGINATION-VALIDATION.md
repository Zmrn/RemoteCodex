# 0.9.8 会话按需加载与跨设备速度验证

本轮已完成开发、打包、tx 更新资源发布、本机和笔记本在线升级。桌面启动器仍为 RemoteCodex.exe，版本显示在软件左下角。

## 修改后的行为

- 首屏最多返回 40 个消息项，消息体目标约 128 KiB；同一轮很长也会分片。单个超长消息保留完整内容，所以页大小不是绝对硬上限。
- 向上滚动到顶部附近时继续拉取一段，也保留“加载更早的消息”按钮。加载期间不重入，失败保留已有内容和输入能力，可以重试。
- 刷新按任务 ID 和消息项 ID 合并，保留已加载历史及可见消息的滚动位置。断线期间新增超过一页的内容会分段补读，避免留下缺口。
- 底层每次只读官方最近 2 轮，用受限的内存查看快照继续分片；跨轮时使用官方返回的历史游标。快照不拥有、不运行任务。
- 图片从文字 JSON 分离成按任务授权的引用，滚动到图片附近时才独立读取原始字节；本轮没有修改图片内容。
- 当前界面不显示的原始工具负载不再随分页视图传输；命令输出维持原界面显示前 6000 字符的行为。完整读取接口保留供现有诊断流程使用。
- 最新消息和设置未变化时返回内容摘要确认，不再反复传整页。
- 双方均需更新到 0.9.8 才能完整减量。连接旧版服务仍可读取，但旧服务仍会发送大响应。

## 真实测量

均经本机回环服务 → 已保存的 Tailscale 笔记本 → 笔记本官方 ChatGPT 桌面接口。只记录时间、数量、字节数、状态与必要 ID，不保存私人正文。

| 测量                       |           0.9.7 |                                  0.9.8 |
| -------------------------- | --------------: | -------------------------------------: |
| 同一真实会话首段 HTTP 读取 |       33,909 ms |                                 410 ms |
| JSON 解压后大小            | 2,627,296 bytes |                           56,084 bytes |
| 新版首段实际压缩传输量     |          未记录 |                           19,793 bytes |
| 新版首段消息项数           |  351（原 3 轮） |                   40（最近一轮的片段） |
| 向上继续读取一段           |          未记录 | 279 ms / 40 项 / 14,239 bytes 压缩数据 |
| 内容未变化的刷新           |          未记录 |                     304 ms / 138 bytes |

新旧采样来自同一个运行中的任务，不是冻结数据的基准测试，网络及任务变化会影响耗时。JSON 大小约减少 97.9%；此次首段 HTTP 用时约减少 98.8%，不是对所有网络环境的延迟保证。

使用实际安装的 0.9.8 页面与服务进行 Edge 浏览器验证，两条笔记本任务从点击到内容出现分别为 1,453 ms 和 1,624 ms，输入均可用；切回本机为 589 ms。页面无 JavaScript 错误，没有任务写入。浏览器使用的就是单 EXE 解压后的页面，不是另外覆盖的源码页面。

本机真实官方所有者另测：首段 54 ms / 40 项 / 27,251 bytes 压缩数据；已缓存的下一段 0 ms（毫秒计时精度）/ 23 项。该组是本机源码调用，不能冒充远端测试。

## 验证结果

| 检查                                   | 状态   | 证据                                                                                               |
| -------------------------------------- | ------ | -------------------------------------------------------------------------------------------------- |
| 自动协议、安全与功能测试               | 已通过 | npm test，46 项通过                                                                                |
| 长轮内分片、跨轮游标、消息顺序和完整性 | 已通过 | 211 项消息跨 5 轮，所有页合并后与输入消息 ID 序列一致；重读同游标结果稳定                          |
| 非法、跨任务、过期游标与缓存上限       | 已通过 | 拒绝错误游标；连续刷新保留正在查看的历史游标                                                       |
| 首屏 40 项与向上按需加载               | 已通过 | 隔离 HTTP 服务 + 真实 Edge UI；没有首屏自动下载整个历史                                            |
| 滚动位置与已加载历史保留               | 已通过 | 翻页前后首个可见消息 ID 一致，像素偏移误差小于 3 px                                                |
| 多页遗漏消息补读、去重                 | 已通过 | 新增 90 项超过首屏容量，自动补齐且无重复；旧页不覆盖新回答                                         |
| 历史读取失败与重试                     | 已通过 | 原内容与输入保留；原游标可以重试                                                                   |
| 图片原字节与任务边界                   | 已通过 | 文字 JSON 不含 base64，另读字节一致；其他任务不能访问该引用                                        |
| 界面与旧端兼容回归                     | 已通过 | 慢请求、连续事件、切设备/任务、SSE 恢复、问答、图片、设置及手机布局相关脚本通过                    |
| 真实两台设备更新                       | 已通过 | 本机和笔记本都返回 currentVersion=0.9.8；本机更新结果 updated；两台官方 ChatGPT PID 均与更新前相同 |
| 单 EXE 自检                            | 已通过 | 内置 Node / Python、DPAPI、静态页面、Win32 管道、真实项目与列表只读访问通过                        |
| 网络中断、锁屏或未来官方版本           | 未测试 | 本轮没有更改网络配置、重启官方应用或制造真实任务故障                                               |

查看快照最多 32 份、累计约 32 MiB，闲置 30 分钟过期。过期或桥接器重启会明确提示重新打开会话，不把缺失数据当作加载完成。缓存只用于查看，官方历史和正在执行的任务不受影响。原图引用缓存也有内存上限，淘汰后需重新读取对应内容。

本轮没有发送任务消息、调整模型/权限、清理官方历史、执行任务中断或修改网络设置。

## 复测与交付

```powershell
npm test
# 设置 REMOTE_BRIDGE_PLAYWRIGHT 为现有 playwright-core 的 index.mjs 文件 URL
node scripts/verify-pagination-ui.mjs
node scripts/verify-device-switch-ui.mjs
node scripts/verify-features-ui.mjs

# SAVED_AGENT_ID 和 TASK_ID 从当前已保存设备、真实任务列表中取得
node scripts/verify-pagination-live.mjs SAVED_AGENT_ID TASK_ID
node scripts/verify-device-switch-live.mjs SAVED_AGENT_ID TASK_ID
```

本地证据位于未提交的 evidence/pagination-\*.json、evidence/device-switch-live.json；基线位于 work/pagination-before.json。测试脚本只保存统计和必要标识，读写回归使用隔离样本。

最终文件：dist/RemoteCodex.exe，43,979,776 bytes。
SHA-256：d0c78efa77d9960b7e12d2f30f9559b7e0a0cb7910e5c5e2c79abbc1fca62042。
本机桌面启动器哈希一致。tx 只保留一份新 EXE 和签名清单，发布私钥没有导出。
