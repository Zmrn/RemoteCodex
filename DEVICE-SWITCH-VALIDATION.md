# 0.9.6 远端会话读取与设备切换

问题来自查看请求和连接恢复流程。并非接入密钥失效：现场的笔记本 `/status` 与 `/threads` 均返回 HTTP 200，耗时约 278–319 ms，能读取 50 条任务；本机对应接口约 2–17 ms。

旧版读取其中一条运行中 Codex 任务时，返回 3 轮、351 个消息项，JSON 约 9 MB，耗时 30.8 秒。另一条任务在 65 秒的探测期限内没有完成传输，记录为超时，不能据此宣称任务已完成或没有内容。探测没有保存私人正文、图片或访问密钥。

旧界面每次读内容都增加序号，只接受最新发起的请求。慢响应遇到持续的所有者事件时，被后续读取反复作废；事件过密时，500 ms 的防抖还会一直延后读取。切换设备只取消事件流，旧内容请求继续占用同源连接。另外，事件流重连收到确认快照后，没有将界面恢复为已连接。选中设备失败也可能跳过错误处理，停在“正在连接”。

修复后的每条会话同时最多发起一个内容读取，将期间的刷新合并到下一次读取。设备和会话各有独立的查看请求取消范围；发送消息、排队、设置等写入不随切换取消或重试。事件采用节流，重连以确认快照恢复连接并补读。加载、格式不支持、超时和内容读取失败有可见提示；读取失败保留未知状态，可手动刷新重试。

新版会话视图避免在 `live.state` 再传一份全量消息和工具输出，保留模型、权限、运行状态和待回答问题；合并后的消息、原图引用和历史分页保持原样。JSON 支持 gzip，两层代理保留压缩协商与响应头，原始附件和 SSE 不改成压缩数据。未协商压缩的旧客户端继续得到普通响应。

| 检查 | 结果 | 实际证据 |
| --- | --- | --- |
| 隔离复现旧版空白 | 已通过 | 每 650 ms 产生事件、读取耗时 2 秒；旧版 6 秒内始终不能显示内容，修复后能显示。 |
| 慢读取与持续事件 | 已通过 | 单个内容读取运行期间合并刷新；100 ms 高频事件下仍持续显示新内容。 |
| 切设备 / 切任务 | 已通过 | 旧读取取消传播到两层代理；新设备与会话不被迟到数据覆盖。 |
| SSE 中断与恢复 | 已通过 | 断连显示未知；确认快照恢复连接、内容和输入能力，无消息写入或重放。 |
| 读取和设备选择失败 | 已通过 | 显示明确错误，退出连接中状态；内容失败可刷新恢复。 |
| 旧版笔记本与新版界面 | 已通过 | 实际显示原运行中任务 3 轮内容，输入可用，耗时 32.6 秒；切回本机 150 ms，50 条任务恢复。此项使用新版页面源码覆盖和真实旧版服务。 |
| 大 JSON 与兼容性 | 已通过 | 两层实际 HTTP 代理测试 gzip 完整解码、消息与问题不丢失；旧客户端和 `gzip;q=0` 保持未压缩。 |
| 关闭时存在 SSE 和慢远端读取 | 已通过 | 当前打包版 9 个所属进程在约 452 ms 内全部退出，再次打开成功；两台官方 ChatGPT 的 PID 均不变，远端仍确认有运行中任务。 |
| 现有功能回归 | 已通过 | 39 项协议与安全测试；状态指示、队列、图片草稿取回、问答、设置、接入设置、深色主题、输入框回缩及手机布局 UI 回归通过。 |

本机桌面已更新至固定文件名 `RemoteCodex.exe` 0.9.6。本机回环端口和保存的设备列表保留；本机 Tailscale 监听仍保持关闭。本轮没有发送任务消息、修改任务设置、执行中断、改动官方应用或网络配置。

证据在未提交的 `evidence/device-switch-ui.json`、`device-switch-live-legacy.json`、`device-fix-lifecycle.json`；只记录必要 ID、数量、状态、耗时，不记录私人正文。此次本机采用正常关闭窗口后替换 EXE，再实测新版的退出流程；不把它描述为旧版自动更新已验证成功。

复测：

```powershell
npm test
# 安装 Playwright Core 或设置 REMOTE_BRIDGE_PLAYWRIGHT 后运行
node scripts/verify-device-switch-ui.mjs

# 真实只读复测：参数取自已保存设备及其任务列表
node scripts/verify-device-switch-live.mjs SAVED_AGENT_ID TASK_ID TASK_ID
```

真实脚本默认使用当前已运行的打包版页面、服务和设备配置。仅抑制保存当前所选设备的偏好写入，并拦截任务修改请求；允许订阅与连接。开发时可设置 `REMOTE_BRIDGE_UI_SOURCE_TEST=1` 覆盖页面源码，与未升级服务验证兼容性。软件启动、停止和构建方法见 `README.md` / `PORTABLE.md`。
