# Windows 窗口尺寸记忆 0.10.2

原先每次启动固定为 1440×960，低分辨率电脑需要手工缩小。现在在每台电脑的本机数据目录保存普通窗口的大小、位置及最大化状态；退出或在线更新后继续使用。最小化保留之前的普通大小与最大化状态，不保存 Windows 的离屏图标坐标。

移动/缩放后 400 ms 合并写入；拖动结束、关闭到托盘、正常退出、更新退出时立即写入。使用临时文件后原子替换，避免中断时留下半份配置。位置文件独立于版本目录，不同步给其他设备，不涉及官方应用数据。

没有历史尺寸时使用当前主屏可用区域的 90%，上限 1440×960。恢复时按保存的屏幕、重叠区域或主屏选择目标；尺寸和位置限制在工作区域内，避开任务栏。支持副屏负坐标、屏幕重新排列、断开副屏和低分辨率回退。损坏的配置使用可见的默认大小。

## 验证

`python scripts/verify-window-placement.py` 在 Windows 本机执行独立进程保存/重载，使用实际 WinForms/Screen 恢复路径，不显示测试窗口，不占用焦点、不操作官方任务。13 项检查通过：跨进程尺寸/位置、最小化保留状态、退出最大化、800×560 低分辨率工作区、断开副屏、负坐标、多屏重新排列、首次小屏尺寸、极小工作区、损坏配置、越界整数、WinForms 手工定位及当前真实屏幕范围。

分辨率变化由测试输入的工作区域模拟，没有修改系统分辨率或用户显示设置。未在用户提到的低分辨率电脑上手工拖动复测。

正式构建通过 Node 63/63、窗口持久化 13 项及 EXE 内置依赖/官方只读连接自检；APK 验签通过，两个平台均发布为 0.10.2。本机经签名更新器升级后，`python scripts/verify-window-placement-live.py` 只读核验窗口句柄所属 PID、实际窗口矩形和新保存配置：均为位置 (240, 52)、大小 1440×928，结果一致（`work/window-placement-live.json`）。这项检查没有拖动窗口或向官方任务发送消息。

## 使用与复测

1. 更新到 0.10.2 后，将窗口调整到喜欢的大小。
2. 从托盘右键退出，再双击启动器，检查尺寸恢复；最大化退出后再次打开也应保持最大化。
3. 缩放到手机宽度时仍使用现有响应式侧栏布局。

配置：默认 `%LOCALAPPDATA%/RemoteCodex/data/window-placement.json`；自定义 `--home` 则使用其 `data` 目录。

正式发布按项目约定同时构建 APK 与 EXE。Android 本次同步版本号；没有改变 Android 窗口或横竖屏逻辑。
