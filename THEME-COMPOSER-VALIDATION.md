# 0.9.5 深色主题与输入框回缩

界面统一为深蓝灰主题：侧栏、消息、代码块、输入框、模型菜单、队列、设备设置和手机布局均使用同一套颜色变量。窗口加载底色及 WebView2 使用深色；支持的 Windows 同时使用深色标题栏。不改变官方 ChatGPT 主题。

输入框旧问题是提交成功后只清空 `value`，保留了输入事件设置的较大内联高度。现在统一在程序修改文字时重算高度，空内容清除内联高度并恢复当前布局默认值。用户输入和窗口宽度变化也使用同一路径；发送失败继续保留文字和高度。

| 验证 | 结果 | 依据 |
| --- | --- | --- |
| 发送长消息后回缩 | 已通过 | 1440×960、390×844、844×390 下清空后高度分别恢复约 63.38、37、51.59 px，与对应初始状态相同。 |
| 运行中发送进入队列后回缩 | 已通过 | 三种布局均验证排队成功后回到空输入框的原始高度。 |
| 从队列取回长草稿 | 已通过 | 取回后自动增高，清空后恢复。 |
| 切换会话 | 已通过 | 空任务不继承前一个任务的高度，返回长草稿任务时恢复正确高度。 |
| 新建任务第一条长消息 | 已通过 | 接受后清空文字并回缩。 |
| 提交失败 | 已通过 | 显示提交结果未知，原文字和展开高度保留。 |
| 深色桌面、菜单和设备设置 | 已通过 | 浏览器断言深色底和可读文字，并人工检查生成截图。 |
| 深色手机竖屏及横屏 | 已通过 | 自适应界面及输入框测试通过，窄屏截图检查通过。 |

验证使用真实网页代码和隔离 HTTP 响应，未向官方任务发送测试消息，也未修改开发会话。证据：`evidence/theme-composer-ui.json` 及 `evidence/dark-theme-*.png`。复测：设置 `REMOTE_BRIDGE_PLAYWRIGHT` 后执行 `node scripts/verify-theme-composer-ui.mjs`。运行 `npm test` 验证桥接器已有协议和安全回归；其他功能 UI 脚本为 `scripts/verify-features-ui.mjs`、`scripts/verify-access-ui.mjs`。

完整源码和构建说明见 `README.md` / `PORTABLE.md`。成品仍为单个固定文件名 `RemoteCodex.exe`，左下角版本 0.9.5。
