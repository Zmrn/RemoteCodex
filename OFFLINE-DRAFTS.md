# 离线草稿编辑

2026-09-16，源码版本仍为 0.10.38；本次没有构建或发布安装包。

原先 `permissions()` 将已有任务的输入权限绑定到已加载的 `taskData`，Chat 还依赖目标端的文字发送能力。断线或最新历史读取失败时，实时数据会被正确清空，却同时禁用了本地输入。于是“仍连接官方桌面、历史刷新失败”也无法继续写草稿。

现在只要目标设备仍存在、已有任务已选中，就可编辑本地文字。Codex 新对话仍可先写草稿；Codex 图片可离线选取、粘贴、移除，保留原格式/数量/大小限制。只添加图片也会触发与文字相同的 200 ms 草稿备份。底部提示当前无法发送、可以继续编辑。

发送条件保持独立：必须有当前官方连接、对应功能能力和允许发送的会话状态。受保护任务不能发送或用 Ctrl+Enter 调整方向；队列接口不兼容而调整方向接口正常时，原有 Ctrl+Enter 功能仍可用。官方 `notLoaded` 任务可编辑带图草稿，但图片不能随文字续接发送，需先在官方打开任务或移除图片。Chat 空白新建与图片功能仍未开放。

继续使用既有设备/任务键、连接身份保护、IndexedDB 快照克隆与串行写入，不保存官方消息、运行或已读状态的副本。重连和历史刷新不改输入，也不自动补发。初始草稿恢复完成前、发送或队列操作尚在提交时仍暂停编辑，避免迟到恢复/回执覆盖新输入。没有更改启动恢复顺序、存储格式或原生客户端数据。

## 验证

- Node 22.19.0 全套 302 项及接口清单通过。
- `scripts/verify-offline-drafts-ui.mjs`：共享页面 1300px/390px 共 12 组，先复现旧版历史失败禁用输入；修复后检查文字/图片编辑、焦点保持、图片单独触发备份、离线重开、重连、设备/任务隔离、只读 Ctrl+Enter、Chat 能力/历史缺失、清空后重开、只在明确点击后发送。已加入 GitHub Checks。
- `scripts/verify-reconnect-ui.mjs`：真实隔离 HTTP/SSE 断线后继续修改草稿，恢复后文字与图片保持；沿用 45 秒静默连接、取消旧设备重连等回归。
- 原 Chat、功能独立兼容、分段历史、草稿删除、侧栏导航、设备切换回归通过。草稿删除测试补上当前统一的合成接口证据，保留全部原断言，不绕过生产能力检查。
- 新测试桌面/手机截图已审阅；结果位于隔离工作树 `work/`。测试只操作合成任务与独立数据目录，未操作用户真实任务、设备设置或已安装客户端；没有执行 APK/模拟器。

修改位于共享 `public/app.js`，后续明确打包时进入 Windows/Android 控制界面。现有已发布 0.10.38 安装包不包含本次修复。
