# 图片放大预览 · 0.9.11

## 2026-09-22：同一路径覆盖后预览更新（已发布0.10.46）

原本本地图片的媒体ID只取决于路径。Agent覆盖文件后再次引用这个路径，ID不变，前端既复用旧节点，也命中旧下载结果；HTTP已有no-store和实际内容ETag，但缓存命中根本没有发起新请求。

目标Windows端在装饰当前官方消息时读取图片的文件身份、大小、纳秒修改/变更时间，生成文件变化标识，再加入官方轮次/消息ID作为`contentKey`。新消息引用即使遇到完全相同的文件元数据也重新读取，避免跨消息旧缓存。授权ID和任务范围保持；Markdown、用户附件、生成图片本地路径，以及rollout回退读取都经同一层处理。同一次历史读取内，同一路径只stat一次，不读取/散列大图正文，不建立持久图片或消息状态。大小相同且mtime恢复时使用ctime等变化；元数据碰撞时以新的官方消息身份保证新预览不复用旧消息字节；原子替换、删除与重建同样更新标识。它是文件版本提示，不是假冒原字节SHA256，下载仍用已有强ETag核验。

前端下载缓存键纳入`contentKey`，与既有DOM复用、断点隔离采用同一版本依据。当前消息引用新版时新建该图的加载状态，迟到旧下载不污染新版；未变化的图片继续复用节点和已解码字节，正文刷新不重下。新增同一路径消息显示新版，点击放大及下载取新版。已经打开的放大图仍保留点击时的独立快照，关闭后重新打开取当前预览，保留原有手势和资源生命周期。

验证：Node22.19.0共358项通过；新`test/image-overwrite.test.mjs`4项验证版本变化和强制固定全部元数据时新消息独立读取、同大小/完全相同mtime的覆盖、原子替换/删除重建、跨任务保护及仅元数据读取。旧源码3项均失败。`scripts/verify-image-overwrite-ui.mjs`用真实隔离文件、生产MessageMedia/HTTP/共享界面，在1300px与390px共10组验证原字节和强制元数据碰撞、放大/下载、新消息独立核验、旧请求迟到和失败恢复；仅换回0.10.45旧app.js可复现下载结果仍是旧图，修复后通过。已纳入GitHub Checks。首次Checks35707974820在快速覆盖并恢复mtime的用例发现元数据相同，证明仅靠stat不足；后续加入官方消息身份隔离和强制全部元数据不变的Node/UI回归，没有跳过断言或重跑原提交。原稳定6组、进度/续传10组、手势4组、GIF2组和Chat UI通过；隔离旧源码→新源码HTTP/UI/DPAPI设备保存、强制重启保留、删除和现有设备配置字节保护通过。手机/桌面截图已审阅。

已于后续打包轮次随0.10.46经GitHub同次双端构建正式发布，包内10组覆盖验证及线上验收见GITHUB-UPDATES.md；上文为源码阶段验证。未运行APK/模拟器、升级现有客户端、重启官方或写用户任务。需要目标Windows接入端及Windows/Android控制端一起更新。老接入端没有本地文件版本提示，不能仅更新控制端解决；第三方HTTPS图片的浏览器缓存策略不在本次范围。官方接口清单和未完成Chat/Work能力未扩展；原8份Chat工作独立保全。

## 2026-09-18：图片重试立即失败与下载进度（源码未打包）

真实问题图不是网络超时。官方Markdown使用`/C:/...`形式的绝对路径，旧版在Windows交给文件系统后变成重复盘符`C:\C:\...`，GET /media约3ms即返回400/ENOENT；共享界面又丢掉HTTP错误详情，只显示“图片加载失败”，重试仍然读取错误路径。

`messageLocalPath`只在Windows处理单个前导斜杠加盘符的明确形式，图片及同类文件附件共用。不会猜测盘符、搜索替代文件、解码URL转义、放宽UNC/设备路径或接受控制端任意文件路径。读取仍必须来自该任务已读取消息登记的媒体ID。源文件不存在与权限错误显示明确原因。

正文附件图片使用共享`image-transfer.mjs`逐块读取HTTP响应：请求中、按真实字节计算的百分比/已下载大小/总大小、下载后解码分别显示。没有总长度或响应有编码时，仅显示实际接收量和不确定进度，不伪造百分比。Android转发原先不提供Content-Length，现对未编码响应保留这个字段，流仍直接转发；Windows转发原本已保留。

错误响应最多读取16KiB，只有JSON中的错误说明会作为文本显示；HTTP状态、找不到文件、超时、网络中断和解码失败分别提示。失败/截断会丢弃这次下载，用户重试发起新请求；同设备/任务/媒体下载仍合并，观察者共享实时进度，不因刷新重建成功图片。清空旧src引起的无源错误事件不再误伤正在重试的加载。HTTPS第三方Markdown图片仍通过浏览器直接加载，不新增带凭据代理或跨域读取；其加载状态保留，不声称可取得第三方站点的字节进度。

`test/image-transfer.test.mjs`覆盖盘符路径、原图/文件字节、跨任务与UNC限制、流式进度/多观察者、缺长度/编码、截断和错误说明边界。`scripts/verify-image-progress-ui.mjs`使用真实分块HTTP响应与生产共享页面，桌面1300px/手机390px共6组，覆盖实际错误、重试、可观察半途进度、无长度、连接中断、缓存及页面布局，已纳GitHub Checks。原图片稳定6组、图片/快捷键7组及Markdown6组回归通过。24份生产Java源码API35编译通过，实际LocalServer.responseLength在主机JVM的定长/分块/带编码HTTP响应下正确且原字节保持，没有构建或执行APK。

当前官方Windows x64 26.908.9136.0的问题任务只读复查：先通过owner只读订阅读到当前图片，再经修复后独立服务器GET读取为200/image/jpeg，335737字节，SHA256 `6fc56dc52245df58339f092939c4cbf424fb973308a2735b7fb5c4e5b2d171c7`与原文件一致；浏览器解码1322×1368。图片内容和私人消息未纳入仓库，任务/已读写入0，未改现有客户端配置。证据见会话work/image-retry-progress/work/。

本轮保持0.10.40版本，只提交源码修复。现有已发布0.10.40包没有本次修复；后续明确打包后，目标Windows接入端和Windows/Android控制端需一同更新。未完成Chat工作继续独立保留。

## 2026-09-14：图片内缩放与拖动（源码未打包）

旧预览只有适应窗口/原始尺寸及滚动，没有图片手势处理或touch-action约束，手机双指手势由浏览器执行为整个页面缩放。现使用共享`public/image-gestures.mjs`接管图片预览内Pointer Events，图片自身变换，工具栏及背后页面保持尺寸。

- 双指捏合按触点距离缩放，并保持手指中点对应的图片位置；放大后单指拖动。双击或连续轻点两次切换2.5倍与适应窗口；“原始尺寸”显示原始像素大小，放大后该按钮用于恢复适应窗口。
- 默认不放大小图；缩放范围为适应窗口至至少8倍，必要时延伸到原始像素尺寸。拖动按可视区域与图像尺寸限制，空白方向居中，不会把整张图拖丢。双指松开一指后重新确定拖动起点，取消/失焦释放手势，避免继续跳动。
- 桌面支持滚轮缩放、鼠标拖动及双击；Esc、关闭按钮、单击空白背景、焦点恢复和原图下载保留。拖动/捏合结束或图片单击不会误触背景关闭。
- 横竖屏改变时适应图继续适应，放大图尽量保持像素倍率并重新限制边界；关闭释放指针与自有Blob，重开恢复适应窗口。同图异步Blob引用切换不会重置正在使用的缩放。

仅打开的图片对话框设置`touch-action:none`并阻止其Ctrl+滚轮页面缩放，不修改整页viewport、不关闭全局可访问性缩放，也不调整Android原生WebView配置。关闭预览后普通页面双指缩放仍可用。Windows服务显式提供新模块，Android打包和本地资源服务沿用`public/`共享路径。

`scripts/verify-image-touch-ui.mjs`使用真实Edge移动视口及CDP触摸输入，覆盖图片缩放/触点位置、单指拖动、双指转单指、取消恢复、双击、缩放与平移边界、旋转、关闭重开、页面visualViewport.scale/背景滚动/草稿、桌面滚轮和原始尺寸。相同脚本对0.10.36旧查看器按预期失败，确认能捕获整页缩放；新脚本纳入GitHub Checks。专用场景使用生产查看器/CSS和隔离图片，原`verify-desktop-ux-ui.mjs`另验证生产服务/共享整页与正文、队列、草稿图片入口及原图字节。

首次Checks34859038566的Node、兼容清单和此前UI通过，新手势在重开图片时失败：测试直接对初始Blob执行decode()，原有独立Blob引用切换使该解码被中止。测试现等待预览自有Blob、有效尺寸及实际加载完成状态，不更改生产加载流程、不跳过重开场景。新手势4组本地复测通过，最终Checks以新提交运行结果为准。

Node22.19.0全套290项、兼容清单、新手势4组、原桌面交互10组、图片/快捷发送7组及Markdown图片6组检查通过。本轮未运行APK、模拟器或安装更新，没有官方任务读写、没有改设备/密钥/草稿格式；保持0.10.36版本，现有安装包不包含本次源码修复。浏览器触摸验证不能替代用户手机WebView实测。

## 0.10.34 已发布（2026-09-11）

下述图片刷新稳定性修复已随GitHub Build34550835116正式发布至[v0.10.34](https://github.com/Zmrn/RemoteCodex/releases/tag/v0.10.34)。最终版本共享UI6组、同SHA Checks、双端包内界面/签名及匿名下载核验通过。未运行APK或升级现有客户端，手机真实行为待用户更新验证。完整记录见GITHUB-UPDATES.md；下面未发布描述保留开发阶段事实。

## 2026-09-11：刷新不再重置已加载图片（未发布）

原页面更新仅保留整轮容器，普通消息和图片节点仍被重新创建。下载缓存命中也会重新进入loading，造成图片与占位高度反复切换。现在按当前设备/任务/轮次/消息的实际内容复用普通消息；正文变化时，同一消息里未改变的附件与HTTPS Markdown图片继续使用原节点。待下载、下载中、成功、失败状态随节点保留，普通刷新不自动重试失败图片。

rollout独立图片新增由具体图片源计算的`contentKey`。文件追加、正文变化、重连导致下载引用换新时，只有新官方结果仍包含同图，才复用已解码节点，并更新原图下载路由；旧下载ID仍受原文件版本校验。图片变更/删除继续服从官方结果，不保留消息副本或本地完成/已读状态。历史版本变化也不再被当成首次打开而强制滚到底。

断线后保留成功图片，取消的下载在恢复时绑定新读取信号。下载缓存存字节，显示节点单独拥有Blob URL，缓存淘汰不再撤销当前图片；节点实际移除后释放其URL，避免每次刷新泄漏。Windows静态路由与Android共享资源均包含新模块`public/image-reuse.mjs`。

`node scripts/verify-image-stability-ui.mjs`使用共享生产页面与隔离API，在1300px/390px覆盖连续刷新逐帧节点和高度、同图换引用、正文变化、新图加载期间刷新、真实取消/重连、失败显式重试、官方删除与资源释放，共6组，已纳入GitHub Checks。既有图片/Markdown/分页/历史/已读/设备隔离回归用于验证相关行为。未执行APK或模拟器、未升级现有客户端；版本保持0.10.33，本节修复尚未构建发布。

## 2026-09-10：加载占位与失败重试（0.10.30 已发布）

已随GitHub同次双端构建34463415359发布；包内容/签名和匿名下载检查通过，未执行APK或升级现有客户端。完整记录见 GITHUB-UPDATES.md。

正文附件、Markdown HTTPS 图片和放大预览共用 `public/image-load-state.mjs`：下载及图片解码期间显示转圈与“图片加载中…”，不显示空 src/未解码图片的浏览器裂图。HTTP 失败、超时或图片解码失败后显示“图片加载失败”和重试按钮；点击重试回到加载状态，成功后才显示原图与下载链接。

正文附件继续在进入阅读区域时加载；成功下载可供刷新复用，失败的缓存会移除，重试重新读取原资源。任务和设备的取消信号在创建图片节点时绑定，迟到的下载结果不能污染切换后的页面；保留原图、任务媒体访问校验与 Android 原图下载路由。这里只维护图片显示状态，不改变官方消息、任务已读状态或草稿。

验证：`node scripts/verify-image-steer-ui.mjs` 覆盖挂起下载、HTTP 503、200 返回损坏图片、连续手动重试、成功缓存、放大、跨设备同 ID 与迟到失败、320/390/844 视口；`node scripts/verify-markdown-images-ui.mjs` 覆盖 HTTPS 图片、失败重试、无 Referrer 和放大。全部为共享生产界面的隔离数据测试，本轮没有运行 APK 或向真实任务写入。

验证日期：2026-09-09，Windows 本机。

## 使用

点击会话、待发送或队列中的图片，打开全窗口深色预览。默认适应窗口，可切换原始尺寸并滚动查看细节；双击图片也可切换尺寸。点击背景、右上角关闭按钮或按 Esc 返回。预览内保留“下载原图”。

仅使用桥接器已经读取的图片，不发送新任务，不生成替代图片。预览会保留自己的临时图片引用，关闭时释放，避免后台刷新缩略图影响正在查看的原图。

## 验证

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 会话、待发送、队列图片打开 | 已通过 | 真实前端 + 隔离 API 数据，分别点击三类缩略图 |
| 键盘操作和关闭 | 已通过 | Enter 打开、Esc 关闭并返回焦点；关闭按钮和背景点击关闭；点击图片本身保持打开 |
| 原始尺寸与下载 | 已通过 | 图片显示宽度与原图宽度一致；预览下载文件字节与 PNG 原件一致 |
| 竖屏、横屏 | 已通过 | 390×844、844×390 视口测试；竖屏截图人工查看无溢出 |
| 既有桌面功能回归 | 已通过 | 更新入口、23% 下载进度、剪贴板图片请求字节、文件下载、队列取回保持通过 |
| 单 EXE | 已通过 | 构建成功；隔离 `--self-test` 确认内置运行时、DPAPI、资源、官方管道和项目/会话只读访问 |
| Windows 原生窗口点击 | 未测试 | 用户此前说明电脑已锁屏或暂时不在电脑旁；本轮使用 Windows 上的无头 Edge 验证前端，没有声称完成原生窗口鼠标实测 |

前端证据：`evidence/desktop-ux-ui.json`、`evidence/image-preview-mobile.png`，使用隔离测试图片，无私人任务写入。自检证据：`work/image-preview-portable-check/data/self-test.json`。

## 复测

```powershell
$env:REMOTE_BRIDGE_PLAYWRIGHT = ([System.Uri](Resolve-Path '../../work/formatter/node_modules/playwright-core/index.mjs').Path).AbsoluteUri
node scripts/verify-desktop-ux-ui.mjs
```

可以将 Playwright 路径换成本机已安装的 `playwright-core`。手动验收时点击一张图片，再试原始尺寸、Esc、下载原图；此操作不向官方任务发送消息。

## 构建

`RemoteCodex.exe`，0.9.11，43,992,064 字节。

SHA-256：`8ffa5d8cc5ef430f39b2a460b27ae03d692286cac3b47c8107b0a706614e648f`。

签名更新源已发布 0.9.11，资源服务器只保留一个 EXE 副本。

本机与已保存的笔记本均通过现有更新入口安装并回读确认 0.9.11，官方 ChatGPT 的 PID 分别保持 131084、10728。桌面 `RemoteCodex.exe` 哈希与上述发布包一致，本机实际服务提供的 `image-viewer.mjs` 与源码逐字一致。安装证据：`evidence/image-preview-release.json`。
# 2026-09-21 图片断点续传

本次将 75 秒整次下载期限改为无数据闲置超时，并保留有强 ETag 的中断片段供重试。原图变化则整图重新下载，最终校验原字节；已加载节点仍保持。下文早期“失败清缓存”指完成结果缓存，不再丢弃可验证的下载片段。范围、内存上限与复测见 [MEDIA-RESUME-ACTIVATION.md](MEDIA-RESUME-ACTIVATION.md)。尚未打包。
