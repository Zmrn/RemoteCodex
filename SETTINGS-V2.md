# 官方设置接口 v1 / v2

2026-09-22：补齐内置权限预设的审批策略和审批者，修复完全访问仍成为自定义组合；新建准备须核对完整有效预设。源码、真实复现与验证边界见 [PERMISSION-PRESETS.md](PERMISSION-PRESETS.md)，本次尚未打包。

2026-09-19：适配 `thread-follower-update-thread-settings` v2，同时保留 v1。已随 0.10.43 经 GitHub 同次双端构建并正式发布；包内与线上验收结果见 GITHUB-UPDATES.md。

## 已核对的官方实现

只读检查本机 Windows x64 `OpenAI.Codex_26.915.4065.0` 安装包：协议表中 settings 为 v2；主进程处理器在未提供 `activeTurnId` 时调用 `updateThreadSettingsForNextTurn(conversationId, threadSettings, condition)`，返回 `{ applied: boolean }`。`condition` 是可选的模型/推理条件。本次不发送 `activeTurnId` 或 `condition`，保持 Remote 原来的“下一轮生效”行为，不修改正在执行轮次的权限。

- 中央清单明确支持 v1、v2；按当前已核对连接的协议观察选择请求版本，不按应用版本号猜测，也不在失败后尝试另一个版本。
- v2 只有同一 owner 回执且 `result.applied === true` 才接受。false、缺失、错误类型或错误 owner 不允许后续消息继续发送；未知派发不会重放。v1 保持既有回执处理。
- 连接身份或管道变化时不得沿用旧观察；未知 v3 及互相矛盾的协议声明继续停用依赖功能，普通默认创建和发送不受 settings 单项异常影响。
- 接口对照页面和生成清单显示 `v1 / v2`，其他接口保持原单版本限制。

## 权限预选与恢复

共享 Windows/Android 权限菜单增加“沿用官方权限”：只删除当前目标草稿的本地权限预选，保留文字、图片、模型及推理设置。新建使用官方默认权限；已有任务保留官方当前权限，不发送权限更新请求。

菜单不依赖模型目录；离线或设置接口不支持时仍可取消本地预选，不支持的三个权限选项禁用。操作时重新检查能力，避免展开菜单后断线仍提交。取消结果持久化到既有草稿备份，刷新不会恢复旧预选。设置回执不替代官方实时状态。

## 验证入口

- `test/settings-versions.test.mjs`：v1/v2 实际转发版本、空闲/运行中设置、新建权限入口、回执拒绝及未知不重放、未来版本隔离、重连与身份变化。
- `scripts/verify-settings-versions-ui.mjs`：桌面和手机布局，保留文字/图片/模型并恢复默认、刷新后保持、运行中设置、回执不伪造显示、离线取消预选。
- 原 `test/settings.test.mjs`、`test/compatibility.test.mjs`、`test/feature-compatibility.test.mjs` 和对应 UI 回归仍适用。

## 2026-09-19 实测结果

用户恢复执行权限后，使用 CI 同版 Node22.19.0 完成 330 项 Node 回归、接口清单、双尺寸权限 UI 6 组，原设置/兼容性/功能隔离/离线草稿 UI 和真实隔离 HTTP/DPAPI 设备保存、强制重启、显式删除均通过。之前受限环境的 EPERM 只记录为环境失败，没有省略保护或降低断言。

当前官方 Windows x64 **26.915.4065.0 / PID9044** 真实连接，21/21 接口匹配、24/24 功能依赖满足。全新独立数据目录创建 2 个登记的专用测试任务（其中 1 个为权限准备），未操作开发任务或既有用户任务：
- 新建任务首轮实时有效权限为工作区权限。
- 运行中经 v2 修改下一轮模型、推理强度和只读权限，3 个设置请求均由同 owner 返回 applied=true；原轮次未被中断。
- 后续轮次实际使用所选模型及只读权限；空闲时权限切换也从官方流确认。
- 同一专用任务补充确认默认速度档位 v2 提交和官方读回。

回归测试不替代全部官方功能实测；当前版本的队列、审批、图片、Chat/Work 写入没有由本次验证，历史 verifiedVersions 不扩大。未执行 APK/模拟器、更新现有客户端、重启官方应用。源证据和报告仅留本机 work/settings-v2/work/，不上传官方代码或私人内容。
