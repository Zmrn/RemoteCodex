# Limit Remote Codex

0.10.49 已修复发送后当前会话不显示回复的问题：目标端只向同一受限配对发送所属会话的最小实时事件，收到新回复后自动刷新。完整版目标接入端与 Limit 控制端都需更新；四包已同次发布，详见 GITHUB-UPDATES.md。

首次添加设备的移动端菜单闪退已在 0.10.48 修复，并加入空设备列表的触控界面回归。完整版和 Limit 版四个安装文件已同次发布；按用户约定，只有明确要求打包时才发布新版。

Limit Remote Codex 是独立安装的受限控制端。Windows 默认使用 `%LOCALAPPDATA%/LimitRemoteCodex`，Android 使用 `com.anso.limitremotecodex`；它们与完整版的设备、密钥、草稿和更新缓存分开。Limit Windows 服务不建立本机官方 ChatGPT/Codex 连接；Android 原本也只连接远端。两端仅接受 `lrc1_` 开头的受限配对码。

Limit Windows 的隔离自检只检查内置运行时、DPAPI 和页面资源，不发现或连接本机官方应用；云端打包门禁会运行最终 EXE 的该项自检。

目标电脑必须运行新版**完整版** Remote Codex，并开启 Tailscale 远程接入。在完整版“编辑这台电脑 → 本机远程访问 → Limit 版配对”中，为一组客户端生成配对码。生成时只显示一次；同一码可填入多台 Limit 客户端，它们共享同一组由该配对身份创建的会话。可以更换码并保留原会话，旧码立即失效；也可以撤销身份，所有使用该码的客户端立即失去访问，但官方会话本身不删除。

目标端只保存配对码的 SHA-256 摘要和“官方会话 ID → 配对身份”的归属记录，使用独立的 `limited-access.json` 及校验副本。存储不可校验时，受限认证和会话操作均停止；不会退化为完整版密钥或空列表重建。创建请求按配对身份命名空间隔离，官方返回会话 ID 时先持久记录归属，再向受限客户端回报。文字和带图新建共用此规则。若创建结果未知，不会猜测归属或自动重发。

远端监听器验证配对码后，目标服务对列表、状态、汇总、历史、图片、附件、队列、发送、设置、审批、重命名和已读回执统一检查归属。目标端只向受限事件流发送其配对身份所属会话的最小状态通知，让打开的会话在回复到达时自动刷新；其他会话事件及官方 owner、连接详情不外泄。配对码更换或撤销时，已有事件流立即关闭。受限配对不能读取项目列表，也不能带 `project` 参数新建，只能创建官方无项目会话。用户选择的会话权限预设及项目less会话内工具能力不因 Limit 版额外改变。完整版密钥仍保留原功能；Limit 客户端拒绝保存或使用完整版密钥。

从 0.10.47 起，用户明确要求构建时，同一次 GitHub Actions 构建输出四个安装文件：`RemoteCodex.exe`、`RemoteCodex.apk`、`LimitRemoteCodex.exe`、`LimitRemoteCodex.apk`。两套 Windows/Android 更新清单分别签名，Limit 客户端只接受对应文件名与 Android 包名。正式发布前应验签、校验包内 edition、保留两版设备数据、核对目标端配对码创建/轮换/撤销与跨身份访问，再确认下载入口；不因提交源码自动打包。0.10.48 四包已发布，未执行 APK 或升级现有客户端。

验证入口：`node --test test/limited-access.test.mjs`、`node scripts/verify-limit-ui.mjs`，完整 `npm test`，`npm run compatibility`。隔离 HTTP 测试覆盖两种配对身份、完整版与受限密钥、项目拒绝、其他会话历史/媒体拒绝、归属记录、换码、撤销及两跳事件流；共享界面回归覆盖桌面和手机尺寸下回复到达后的自动显示。0.10.48 正式包已验签和核对内容，但不包含后续实时回复修复；实际 Android 设备仍待用户安装新版后验证。
