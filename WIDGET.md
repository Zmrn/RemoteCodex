# Android 2×2 任务概览（0.10.17）

## 0.10.18 全部设备持续连接

0.10.17 的目标 Windows 桥接已有自身 IPC 重连，但 Android 的完整查看重连仅跟随当前所选设备；统计只轮询 summary，主页面离开后依赖系统周期 Job。用户查看一台设备时，其他设备恢复不够及时。0.10.18 将所有设备的统计同步独立到 Android 前台服务，与当前设备、WebView、任务和小组件实例数量无关。

- 打开应用/汇总页后默认启动，通知栏显示“全部设备同步”。帮助与更新里的“保持所有设备连接”可开关，通知也可暂停；暂停的是持续服务，当前查看与手动/系统周期刷新仍可使用。
- 每台设备独立获取 status，必要时调用幂等 connect，再读取 summary。正常约每 15 秒同步，失败按 1/2/4/8/15/30 秒持续退避；在线设备结果到达即保存和显示，不等待失联设备。多个设备并发数有上限，较多设备可能排队。
- 不复用页面选择，不重放任务消息/队列/中断等指令；正常连接不重建官方 owner。地址或密钥改变后旧响应丢弃，设备删除后退出重试，切换任务继续保留草稿与已读隔离。
- connectedDevice 前台服务用于与用户保存的外部电脑进行网络连接。声明 FOREGROUND_SERVICE、FOREGROUND_SERVICE_CONNECTED_DEVICE、CHANGE_NETWORK_STATE（平台启动前提），代码不修改网络配置。对系统后台启动限制保留失败提示；网络变化触发重试，开机/应用更新尝试恢复已启用的服务，不绕过用户强制停止或省电策略。
- 数字含义及官方列表 50 项上限保持不变。持续服务下超过 60 秒未成功更新的数据标为缓存；手机/电脑断网、远端程序退出、系统停止服务时不能声称实时在线。

复测：`python scripts/verify-device-connections.py` 在主机 JVM 执行生产 DevicePoller / SummaryConnection，验证一台阻塞不影响另一台持续读取、后台独立重试、移除/暂停、退避和恢复，以及 status→connect→summary 的只读恢复链。其 JSON 测试依赖只放 work/，SHA-256 固定，不进入 APK。共享开关由 `test/device-connections.test.mjs` 验证。

本轮延续用户不在连接设备上测试 APK、更新后自行验证的安排，没有安装/执行 APK；JVM 和浏览器检查不能验证 Android 前台服务生命周期、通知展示、桌面刷新、省电或开机行为。没有实施上一轮仅要求预览的汇总页设计稿。

2026-09-09 发布前检查通过：148 项 Node、13 项窗口、12 项主机 JVM 连接恢复检查、共享界面 1300/390px 的已读与跳转/草稿回归、原有真实 HTTP/SSE 故障重连（含45秒静默连接检测）、最终 EXE 包内运行时/DPAPI/官方只读自检、双端签名/原证书和兼容清单元数据核验。APK 编译后的 manifest 确认包含非导出的 DeviceSyncService、connectedDevice 类型及所需权限。

以下 0.10.17 的后台约15分钟刷新机制为历史基线；0.10.18 优先使用上述持续服务，系统周期 Job 保留为补充。

桌面长按空白区域 → 小组件 →「Remote Codex · 任务概览」。深色圆角卡片以橙色显示未读回报数，蓝色显示运行中任务数，布局为 `3 / 5`，合计手机中保存的全部设备。目标 Windows 接入端也需升级至 0.10.17；旧端不支持统计时显示部分统计或暂无数据。

点击橙色/蓝色区域进入跨设备汇总列表，再点击任务进入对应设备与 Codex/Chat 模式。跳转沿用共享界面的设备与任务切换，不清空草稿。点击卡片底部可请求刷新。没有设备时为 `0 / 0`，从未成功获取数据时为 `— / —`，不能把连接失败显示成健康的零。

## 计数与已读

- 每个任务最多计一次；同一官方 `kind + id` 出现在多个设备时去重。运行中任务只计右边，包含等待用户/审批的尚未回报任务。
- 回报指最新一轮已完成且存在非空助手回复。最新一轮已停止、失败、只有工具输出或仍在运行时，不计新的已完成回报。Codex 的 notLoaded 等未知运行状态明确标为部分统计；其可读的已完成历史只能提供历史回报证据，不能证明实时状态，已有缓存优先保留。Chat 还要求官方 renderer 返回 idle，不能只用其合成的历史 completed 字段。
- 未读定义为没有 Remote Codex 已读回执的最新回报。Windows 或 Android 控制端打开对应任务，回报进入可见区域且滚至底部、页面在前台至少 800 ms，才提交该回报的精确哈希。仅打开应用、小组件或汇总列表不会清零。在官方桌面读过不会清除这里的计数。
- 首次启用没有此前的 Remote Codex 已读记录，因此官方列表中已有的最新已完成回报也会计入。每次真正读到后保存；新一轮完成会重新计入。
- 回执保存在目标 Windows 的独立 `data/task-receipts.json`，只保存任务 ID 和回报哈希，不存消息正文。使用系统文件锁、校验摘要、临时文件 fsync 和原子替换；读取错误或校验失败保留文件并报错。旧回报的迟到回执不能清除新回报，也不能撤销较新的已读记录。
- Android 的统计与已读水位保存在独立的私有 SharedPreferences，刷新失败保留最后一次成功结果，精确 token 防止较旧的网络响应反写已读状态。设备列表、地址和 Keystore 密钥使用原有存储，不另建配置副本，不用统计缓存写回设备设置。

## 刷新与限制

Android 后台按约 15 分钟周期申请刷新，系统省电、网络与任务配额可能推迟；没有实时刷新保证。主应用在前台且存在小组件时约每 30 秒刷新，进入汇总页或点击刷新可主动请求。所有小组件实例共用一次多设备统计；没有常驻前台服务或精确闹钟。

页脚显示上次成功统计时间。设备断线时保留缓存并标注；部分设备从未成功连接、官方历史读取失败、状态未知或列表达到上限时显示「部分统计」。未知任务保留已有缓存并标注，缓存不能作为新的实时状态。设备删除或地址/密钥改变后，旧统计不能作为该配置的结果。

官方 list_threads 当前最多返回最近 **50 个未固定任务，加全部固定任务**，没有下一页参数；达到 50 时无法证明全量统计，必须显示部分统计。归档任务不计入。最新一轮超过官方约 8 MiB 通道限制或读取超时也会导致部分统计。本版本没有扩大官方支持版本，没有读取/改写官方历史数据库或官方已读状态来绕过这些限制。

本轮采用 Remote Codex 自有已读定义。不同目标桥接器各自持久化回执；共享同一目标的 Windows/Android 客户端互通已读状态。同一任务从多个目标出现时，相同回报任一目标已读即可在本次合计中去重清除。

## 验证入口和范围

```powershell
node --test test/task-reports.test.mjs
node scripts/verify-widget-ui.mjs
node scripts/verify-agent-restart.mjs
python scripts/build-release.py
python scripts/verify-compatibility-release.py
```

单元测试覆盖回报识别、新旧回执竞态、两个存储实例并发、重建实例持久化、坏文件及保存中断、过期/错误任务回执、只读统计参数、列表去重与截断、未知状态和断线。共享界面测试使用独立假官方 API 和真实本地 HTTP/回执文件，不向用户真实任务写入。

2026-09-09 发布前通过：145 项 Node 回归（含 10 项任务回报测试）、13 项 Windows 窗口检查、1300/390px 共享界面的已读可见性与任务跳转/草稿隔离、0.10.16 历史容错回归、隔离设备增删与强制重启保护、最终 EXE 包内运行时/DPAPI/官方只读自检、双端签名清单及兼容元数据检查。官方 26.903.8094.0 的只读统计可返回结果，并对 notLoaded 等未知状态标为部分统计；没有发送任务消息、停止任务或清除用户任务的已读状态。当前笔记本的 broker 为官方独占。

用户于 2026-09-09 明确要求本轮不运行 APK 设备测试，直接发布后由用户更新验证。**本轮 APK 仅做编译、签名/证书、包元数据和同版本兼容清单检查；没有运行模拟器或物理手机，没有实际验证桌面启动器的 2×2 排版、PendingIntent 点击、后台 JobScheduler、系统回收恢复。** 共享 JS 在浏览器中的窄屏验证不是 APK 实机验证。

官方支持范围仍为 Windows x64 OpenAI.Codex 26.901.6511.0、26.903.8094.0 的 Codex 核心读写，具体限制以中央清单为准。Chat 列表/历史支持，文字续写待真实专用任务验证，新建/模型/图片未支持；Work 未独立验证。VS Code 共享 broker 的真实写入证据仍限原开发设备的官方 26.903.8094.0 + VS Code 1.136.2 + Codex 扩展 26.903.61454 组合。

## 正式发布与本机更新

2026-09-09，0.10.17 已通过标准双端发布器发布，线上两份签名清单、固定发布说明哈希和完整产物哈希核对通过。EXE 44,065,280 字节，SHA-256 `54a3d28973100fe571810d17ee06b89dd84e861f7ca2e3f9623bd4174b82a255`；APK 220,151 字节，SHA-256 `24266d2c496262361f1c0abbf41f47a1f7052ad281b9ca84755790170973e15f`。原 Android 安装证书 SHA-256 保持 `3c0a98ec3c9f37318525f5d0e4afb3417812215d625e48a9013b5ee649acb2b1`。

双端兼容清单 SHA-256 `295e6462068b538a20acde75cf8c629ed1b000c92a5e0174734ca8432816d01c`。笔记本已由内置更新器从 0.10.16 升至 0.10.17；更新缓存使用与线上签名/哈希一致的本地 EXE，仍由更新器验签安装。更新前保全配置，安装后现有 2 项设备、选择、加密密钥、接入及更新设置保持一致，独立历史保留，官方连接恢复且 PID 未变。没有安装或测试 APK。
