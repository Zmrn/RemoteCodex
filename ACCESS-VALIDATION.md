# 0.9.4 远程接入状态与错误提示验证

## 修复

接入帮助页此前只在真正监听时显示端口，未启用时仅显示 IP，容易误以为复制密钥后即可连接。现在始终显示访问端口，并明确区分“尚未监听”和“正在监听”；“设置本机接入”直接打开本机设备编辑界面。打开设置不自动启用接入，仍需要用户勾选并保存。

远端连接提示区分 TCP 连接被拒绝、连接超时和 HTTP 密钥验证失败。写入结果未知时仍不自动重试。读取更新状态失败现在保留实际错误，不再自动推断目标软件版本太旧。

## 结果

| 项目 | 结果 | 实测依据 |
| --- | --- | --- |
| 未启用时显示 IP 和配置端口 | 已通过 | 真实 UI 加隔离响应，使用非默认端口 43210，确认显示“尚未监听”。 |
| 设置入口与保存后状态 | 已通过 | 点击设置入口进入本机设备编辑；仅显式保存后发送启用请求，状态显示实际监听地址和修改后的 43211 端口。此测试不开放真实网络入口。 |
| 窄屏布局 | 已通过 | 390×844 下状态、端口和设置按钮可见。 |
| 密钥鉴权、传输、错误类别 | 已通过 | 36 项自动测试通过，含真实回环 HTTP 鉴权、转发和响应断开；密钥失败返回 401，不与 TCP 超时混淆。 |
| 目标笔记本 Tailscale 连通 | 已通过 | 当前主机查看目标为在线；Tailscale ping 经 DERP 返回 pong，约 19 ms。 |
| 目标笔记本桥接端口 | 失败 | 对截图中目标的 43128 TCP 端口连接在 6 秒后超时。截图同时明确显示目标远程接入未开启。TCP 超时本身不单独证明唯一原因。 |
| 目标笔记本密钥、官方任务读写 | 未测试 | TCP 尚未连通，当前主机没有保存该设备的连接配置，尚未进入鉴权或官方任务读取阶段。需要先在目标电脑启用接入。 |

本轮未修改 Tailscale、系统代理、DNS 或防火墙，未开启当前主机的远程监听，未读取或传输用户访问密钥。真实网络探测只针对用户提供的目标 IP 和端口。其他设备的启用状态不由控制端保存 IP 或密钥决定。

本机证据：`evidence/access-ui.json`、`evidence/access-ui-enabled.png`、`evidence/access-ui-disabled-mobile.png`、`evidence/remote-access-target-tcp.json`。真实地址与截图留在本机，不提交仓库。

## 使用与复测

在被控制电脑运行 RemoteCodex，左下角设备菜单编辑“这台电脑”；选择它自己的 Tailscale IP、访问端口和密钥，勾选允许其他设备通过 Tailscale 连接并保存。看到“正在监听”后，再到控制端填写相同的 IP、端口及目标电脑密钥。需保持目标电脑软件打开或最小化。

源码验证：`npm test`；将 `REMOTE_BRIDGE_PLAYWRIGHT` 设置为本机 playwright-core 模块路径后运行 `node scripts/verify-access-ui.mjs`。单 EXE 构建及更新方法见 `PORTABLE.md`。
