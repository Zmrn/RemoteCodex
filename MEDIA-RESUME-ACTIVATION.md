# 图片续传与已有任务加载

2026-09-21 源码修复，版本仍为 0.10.43；尚未构建或发布安装包。实际生效时需要目标 Windows 接入端与控制端一起更新。

## 图片下载

- 原来的 75 秒限制覆盖整次下载，即使一直收到数据也会中止。现在是无数据闲置 75 秒超时，收到数据后重新计时，进度继续显示实际字节。
- 中断后保留已经收到、具有强 SHA256 ETag 和明确总大小的字节；重试通过 Range / If-Range 请求剩余部分。Windows 两次转发及 Android 原生 HTTP 都传递所需头部。
- 断点仅在当前页面内存中保留，按设备、任务和图片隔离。总计最多 96 MiB，保留 30 分钟，单图上限 64 MiB；内存淘汰、页面/应用重启后需要重新下载。并非持久离线缓存，也不存任务状态。
- 目标重新读取并校验当前任务的媒体授权。原图变化时返回完整 200，舍弃旧片段；206 的范围、长度、类型与 ETag 必须相符，最后核验完整 SHA256。没有可靠验证信息的旧目标保持整图下载，不拼接不明来源片段。
- 已加载图片节点仍复用，刷新正文不使其反复回到 loading。第三方 HTTPS 直链仍走原浏览器路径。

## 未加载任务

- 原来选择任务只读取历史与订阅；官方未创建 owner 时，订阅返回 `no-client-found`。现在仅对当前选中的未加载 Codex 任务请求 `/threads/:id/activate`，不会批量打开侧栏任务。
- 激活使用现有官方 `navigate_to_codex_page`。这会切换目标设备官方应用显示的任务，效果相当于在那里点开任务；不发送占位消息、不启动另一个执行器，也不修改官方数据库。
- 导航回执不等于成功。必须在同一连接、身份、IPC 与订阅下取得新鲜的匹配 owner，且 `resumeState=resumed`、运行状态明确为 idle/active 才完成。最长等待 20 秒；失败可显式重试，刷新不会重复导航，切换页面后不采纳迟到结果。
- 加载期间可继续编辑文字和图片；不会自动发送。加载后重新读取官方状态才恢复发送。带图片、权限或速度的发送及设置修改共用激活准备，后续仍执行原有身份/owner/状态校验及提交去重；未知写入回执不重放。
- 旧目标未声明 activate 时不调用新端点，原文字续接保持。官方状态不明、异地主机、Chat/Work 不使用 Codex 激活。兼容清单只增加由现有五项指令组成的功能依赖，没有虚构新官方协议或扩大 verifiedVersions。

## 验证

- Node 22.19.0 全量回归；`test/image-resume.test.mjs` 覆盖真实 HTTP 双转发、断点与原字节一致、原图变化、错误范围/长度/哈希/编码、闲置超时和缓存隔离；`test/thread-activation.test.mjs` 覆盖并发去重、新鲜 owner、失败/取消/连接变化、带图与设置、未知写入不回退。
- `scripts/verify-image-progress-ui.mjs --slow-transfer`：桌面 1300px/手机 390px 的进度、续传、失败与原图变化，另有真实 HTTP 持续超过 78 秒仍成功的检查。相关稳定节点、图片手势、GIF 回归通过。
- `scripts/verify-thread-activation-ui.mjs`：两种尺寸下单次激活、加载时编辑、失败重试、切任务隔离、旧目标兼容；设置、功能兼容、离线草稿和 Chat 原回归通过。
- `scripts/verify-media-resume.py`：Android 生产 MediaHeaders 在主机 JVM 上经真实 HttpURLConnection 验证，25 份生产 Java 源码以 API35 编译；未运行 APK/模拟器。
- `scripts/verify-agent-restart.mjs`：隔离 HTTP/UI/DPAPI 设备保存、强制结束测试子进程后的保留、显式删除和当前安装的设备/接入配置字节不变。
- 本机官方 Windows x64 26.915.4065.0：通过真实生产 HTTP activate 端点，将已登记的专用 Probe 从 notLoaded 激活为 resumed/idle，约 3.49 秒；仅一次导航，消息写入和请求记录均为零。21/21 接口和 25/25 功能依赖匹配，不代表所有功能行为均已重测。
- 用户指出的任务当时已手动激活，未对其写入或测试。没有重启官方应用、更新已安装客户端或验证本次断开 RDP 的冷加载；官方调度若仍受系统会话限制，界面会保留草稿并显示实际失败，不能承诺覆盖所有 RDP 场景。

本机现场证据保留于隔离工作树的 `work/`，不上传官方源码、私人消息或用户配置。原主工作区 8 份未完成 Chat 修改独立保全。
