# GIF 接收、预览与原图保存

2026-09-16 源码修复，Remote Codex 版本仍为 0.10.37；本轮没有生成或发布安装包。

官方回复中的本地 GIF 图片链接此前没有进入图片识别名单，后续普通文件链接解析将其拆成了 `!` 加图片说明及下载卡。实际问题文件另有 28,482,324 字节，超过原来 25 MiB 图片读取上限，因此只补扩展名仍不能显示。

接收链路现在支持 GIF87a/GIF89a：官方回复的 Markdown 图片、已读取的用户图片附件、本地图片、内嵌 data URI、历史分页的延迟图片引用。返回 `image/gif` 和完整原始字节，不转 PNG、不抽取第一帧。共享 Windows/Android 页面沿用原有加载提示、显式重试、稳定节点和图片内缩放拖动；本地 GIF 的显示说明与下载文件名分开，下载保留原始 `.gif` 文件名。

GIF 接收和二进制原图保存上限为 64 MiB，PNG/JPEG/WebP 仍为 25 MiB。大小按实际文件签名判断，伪造扩展名或 data URI MIME 不能获得更高上限；超限图片提示失败。接收 ID 仍限于当前已读取任务，不能用另一任务或任意路径取文件，UNC/设备路径拒绝。发送图片的格式、单张和批次限制没有改变。

Android 的已登记原文件下载继续走原流式路径；Blob 回退使用二进制接口和 `ReceivedImage` 检查，保存 MIME 包含 GIF。Windows 的 CSP 允许读取页面自己创建的 Blob，与 Android 原有策略一致，使放大预览可保留独立原图引用；没有增加外部网络目标。

## 验证入口

- `test/gif-media.test.mjs`：回复/附件解析、原字节与签名、任务隔离、伪格式和 UNC 拒绝、超过 25 MiB 的 GIF、64 MiB 上界及其他格式原上界。
- `test/rollout-history.test.mjs`：GIF 经官方历史页延迟登记读取，不在页面中复制整张图片。
- `scripts/verify-gif-ui.mjs`：生产媒体 HTTP 和共享页面，1300px/390px 的真实动画帧变化、加载/失败/重试、刷新保留节点、放大、原文件名与下载字节。Android 保存调用在浏览器中截获核验，不等于原生系统保存面板实测。
- `scripts/verify-received-image.py`：只编译运行主机 JVM 格式与大小回归，不生成 APK；Checks 自动执行此项和 GIF UI。
- 原图片手势、稳定性、下载专项继续回归。

真实只读对照使用当前官方 26.908.9136.0 的工具读取加 owner 合并结果：问题 GIF 从 `0 张图片 + 1 个文件` 变为 `1 张图片`，读取 SHA256 与官方原文件一致。真实任务无写入。私人图片和正文仅用于本地忽略目录的验证，不提交到仓库或上传 GitHub；CI 使用代码生成的双色循环动画。

后续明确打包时需同时更新目标 Windows 接入端和控制端。当前已安装 0.10.37 不含本修复。本轮不适配官方 settings v2，也不扩大未完成 Chat/Work 功能。
