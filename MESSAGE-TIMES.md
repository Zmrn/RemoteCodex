# 消息时间

2026-09-11：Windows 与 Android 共享会话界面，在每条用户消息、AI 回复及可见问答记录下方常驻显示日期和时间，格式为 `YYYY-MM-DD HH:mm:ss`，使用当前控制端的本地时区。AI 回复时间与复制按钮同排，用户消息时间在气泡下右对齐，手机无需悬停。

时间只来自官方记录：

- 官方超长历史的 `item_completed` 记录自带 `timestamp` 时，保留到分段页面，标记“记录于”。这是该条完整消息的官方记录时间，不冒充开始生成或发送时间。
- 其余读取路径使用原已返回的 `turn.startedAt`（官方工具的秒；owner 的 `turnStartedAtMs` 沿用现有转换），明确标记“本轮开始”。Chat 共用相同显示，但不推测单条回复时间。
- 缺失或无效时显示“时间未知”，不使用本地接收时间、刷新时间、文件修改时间或任务更新时间补造。没有新的持久化记录，也不修改官方消息、已读或运行状态。

时间更新独立于正文渲染签名，已有图片节点和已解码状态保留；分页沿用同一项的官方时间。只读取已有字段，不新增官方工具/IPC 指令或版本限制。

复测：`node --test test/rollout-history.test.mjs test/conversation-pages.test.mjs`；共享界面使用隔离 API 在桌面和手机宽度检查时间可见性、时区、刷新和分页，并运行 `scripts/verify-image-stability-ui.mjs` 验证图片稳定性。界面检查不代表执行 APK。

本轮 Node 22.19.0 全套 278 项、接口清单、原图片稳定 UI 6 组及 Chat UI 通过。隔离时间界面在 1300px/Asia-Shanghai 和 390px/America-New-York 检查了跨天转换、无悬停可见、缺失时间、刷新更正时间时图片与正文节点保留、较早分页；已检查双宽度截图，无横向溢出。没有读取或写入真实官方任务。

旧接入端未返回单条记录时间时，新控制端仍可显示已有的“本轮开始”；若要显示长历史单条“记录于”，接入端也需升级到包含此改动的后续版本。

本次源码保持 0.10.34；未构建、发布、安装或执行 APK，等待用户明确打包。普通 Chat 新建、生成图片等未完成工作独立保留。
